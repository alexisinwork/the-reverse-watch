# Quiz v3 and Sheet-Native Catalogue Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the version-2 progressive diagnostic with a thirteen-question flow whose every question maps onto a column of the owner's model intake sheet, and make that sheet the canonical catalogue intake format.

**Architecture:** Three sheet columns (wearing scenario, complications, positioning) become PostgreSQL vocabularies read at runtime, so the owner extends them without code changes. New catalogue fields land additively first; the questionnaire and engine are built alongside v2 and swapped at task 12; v2 is deleted at task 15. Every task leaves the build green.

**Tech Stack:** TypeScript (strict), React Router v7 Framework Mode, Zod v4, Vitest, PostgreSQL/Supabase via narrow read-only RPCs, ExcelJS for the workbook export.

**Spec:** [`docs/superpowers/specs/2026-09-03-quiz-v3-design.md`](../specs/2026-09-03-quiz-v3-design.md)

## Global Constraints

- Node.js >= 22.22.0. Run `npm run check` (format, lint, types, unit tests, build) before every commit.
- Zod v4 idioms only: `z.url()`, `z.iso.datetime()`, `z.iso.date()`, `.strict()` on every object schema, `context.addIssue({ code: "custom", path, message })` in `superRefine`.
- Missing facts remain `null`. Never supply plausible-looking defaults. `null` never satisfies an active hard filter.
- Every accepted factual claim retains field-level source provenance via the `evidence` array.
- Brands provide context; the materially homogeneous reference variant is the unit of filtering and ranking. Never score a brand-level rollup as a reference fact.
- Migrations are additive and reversible. Never drop, truncate, or reset a table. Never remove a v3 RPC while adding v4.
- Public catalogue tables keep row-level security with no direct browser grants. The application reads only narrow, read-only RPCs.
- User-facing copy avoids vendor names, implementation plumbing, and empty labels ("timeless", "iconic", "grail", "legendary").
- Do not run Playwright/E2E suites until task 17.
- Commit messages end with:
  `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`

---

## File Structure

**Created:**

| Path | Responsibility |
| --- | --- |
| `app/domain/catalogue-vocabulary.ts` | Vocabulary kinds, slug enums, bundled seed rows with Russian source aliases |
| `app/domain/catalogue-vocabulary.test.ts` | Seed integrity, alias uniqueness, resolver behaviour |
| `app/domain/catalogue-vocabulary.server.ts` | Supabase vocabulary read with bundled fallback |
| `app/domain/catalogue-vocabulary.server.test.ts` | Fallback, timeout, malformed-response handling |
| `app/domain/sheet-intake.ts` | Parse one sheet row into a structured intake record |
| `app/domain/sheet-intake.test.ts` | Parser unit tests per column |
| `app/domain/sheet-dedup.ts` | Reference normalisation, duplicate collapse, conflict rejection, artifact drop |
| `app/domain/sheet-dedup.test.ts` | Dedup unit tests |
| `app/domain/questionnaire-v3.ts` | Version-3 questionnaire contract |
| `app/domain/questionnaire-v3.test.ts` | Schema and derivation tests |
| `app/components/positioning-facet.tsx` | Result-page positioning chips |
| `app/components/positioning-facet.test.tsx` | Facet rendering and empty-vocabulary behaviour |
| `scripts/import-model-sheet.ts` | CLI: sheet TSV to catalogue rows |
| `scripts/migrate-seed-catalogue-v3.ts` | One-shot forward migration of the 71 reviewed variants |
| `data/catalogue/positioning-map.json` | Reviewable phrase-to-group mapping |
| `db/migrations/0065_add_catalogue_vocabulary.sql` | Vocabulary table, join tables, variant columns, seeds |
| `db/migrations/0066_add_recommendation_v4_rpcs.sql` | v4 catalogue and hard-filter RPCs |

**Modified:**

| Path | Change |
| --- | --- |
| `app/domain/catalogue.ts` | Add sheet fields; delete `dateStatus` and `eligibleEnvironments` at task 15 |
| `app/domain/recommendation.ts` | Add v3 engine alongside v2; delete v2 at task 15 |
| `app/domain/catalogue.server.ts` | Point at v4 RPCs |
| `app/domain/catalogue-parity.ts` | Version-3 golden profiles |
| `app/domain/evaluation-fixtures.ts` | Version-3 golden profiles |
| `app/routes/quiz.tsx` | Six screens replacing thirteen |
| `scripts/build-watches-models-workbook.ts` | Emit the real new fields |
| `data/catalogue/seed-catalogue.json` | Migrated by task 8 |

---

### Task 1: Vocabulary domain module

**Files:**

- Create: `app/domain/catalogue-vocabulary.ts`
- Test: `app/domain/catalogue-vocabulary.test.ts`

**Interfaces:**

- Consumes: nothing.
- Produces:
  - `VOCABULARY_KINDS = ["wearing_scenario", "complication", "positioning_group"] as const`
  - `type VocabularyKind = (typeof VOCABULARY_KINDS)[number]`
  - `vocabularyRowSchema` — `{ kind, slug, labelEn, sourceAliases: string[], sortOrder: number, active: boolean }`, `.strict()`
  - `type VocabularyRow = z.infer<typeof vocabularyRowSchema>`
  - `BUNDLED_VOCABULARY: readonly VocabularyRow[]`
  - `normalizeSourceToken(value: string): string`
  - `resolveVocabularySlug(kind: VocabularyKind, token: string, rows?: readonly VocabularyRow[]): string | null`
  - `splitScenarioTokens(cell: string): string[]`
  - `splitComplicationTokens(cell: string): string[]`

- [ ] **Step 1: Write the failing test**

Create `app/domain/catalogue-vocabulary.test.ts`:

```typescript
import { describe, expect, it } from "vitest";

import {
  BUNDLED_VOCABULARY,
  normalizeSourceToken,
  resolveVocabularySlug,
  splitComplicationTokens,
  splitScenarioTokens,
  vocabularyRowSchema,
  VOCABULARY_KINDS,
} from "./catalogue-vocabulary";

describe("bundled vocabulary", () => {
  it("parses every bundled row", () => {
    for (const row of BUNDLED_VOCABULARY) {
      expect(vocabularyRowSchema.parse(row)).toEqual(row);
    }
  });

  it("keeps slugs unique within a kind", () => {
    for (const kind of VOCABULARY_KINDS) {
      const slugs = BUNDLED_VOCABULARY.filter((row) => row.kind === kind).map(
        (row) => row.slug,
      );
      expect(new Set(slugs).size).toBe(slugs.length);
    }
  });

  it("never maps one source alias to two slugs of the same kind", () => {
    for (const kind of VOCABULARY_KINDS) {
      const aliases = BUNDLED_VOCABULARY.filter(
        (row) => row.kind === kind,
      ).flatMap((row) => row.sourceAliases.map(normalizeSourceToken));
      expect(new Set(aliases).size).toBe(aliases.length);
    }
  });

  it("covers all three kinds", () => {
    for (const kind of VOCABULARY_KINDS) {
      expect(
        BUNDLED_VOCABULARY.some((row) => row.kind === kind),
      ).toBe(true);
    }
  });
});

describe("normalizeSourceToken", () => {
  it("lowercases, trims, and collapses whitespace", () => {
    expect(normalizeSourceToken("  Смарт   Casual ")).toBe("смарт casual");
  });

  it("normalizes unicode width and case", () => {
    expect(normalizeSourceToken("ОФИС")).toBe("офис");
  });
});

describe("resolveVocabularySlug", () => {
  it("resolves a known Russian scenario alias", () => {
    expect(resolveVocabularySlug("wearing_scenario", "офис")).toBe("office");
  });

  it("resolves case- and space-insensitively", () => {
    expect(resolveVocabularySlug("wearing_scenario", "  Дайвинг ")).toBe(
      "diving",
    );
  });

  it("returns null for an unmapped token", () => {
    expect(resolveVocabularySlug("wearing_scenario", "марсоход")).toBeNull();
  });

  it("does not resolve a token belonging to another kind", () => {
    expect(resolveVocabularySlug("complication", "офис")).toBeNull();
  });

  it("resolves complications including the time-only marker", () => {
    expect(resolveVocabularySlug("complication", "нет (Time-only)")).toBe(
      "time_only",
    );
    expect(resolveVocabularySlug("complication", "хронограф")).toBe(
      "chronograph",
    );
  });
});

describe("token splitting", () => {
  it("splits scenarios on slashes", () => {
    expect(splitScenarioTokens("офис / спорт-шик / smart casual")).toEqual([
      "офис",
      "спорт-шик",
      "smart casual",
    ]);
  });

  it("splits complications on commas", () => {
    expect(
      splitComplicationTokens("дата, второй пояс GMT, безель 24ч"),
    ).toEqual(["дата", "второй пояс GMT", "безель 24ч"]);
  });

  it("drops empty segments", () => {
    expect(splitScenarioTokens("офис //  / вечер")).toEqual([
      "офис",
      "вечер",
    ]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/catalogue-vocabulary.test.ts`
Expected: FAIL — `Failed to resolve import "./catalogue-vocabulary"`.

- [ ] **Step 3: Write the implementation**

Create `app/domain/catalogue-vocabulary.ts`:

```typescript
import { z } from "zod";

export const VOCABULARY_KINDS = [
  "wearing_scenario",
  "complication",
  "positioning_group",
] as const;

export type VocabularyKind = (typeof VOCABULARY_KINDS)[number];

const slugSchema = z
  .string()
  .regex(/^[a-z0-9]+(?:_[a-z0-9]+)*$/, "Use a lowercase snake_case slug.");

export const vocabularyRowSchema = z
  .object({
    kind: z.enum(VOCABULARY_KINDS),
    slug: slugSchema,
    labelEn: z.string().trim().min(1).max(80),
    sourceAliases: z.array(z.string().trim().min(1)).max(24),
    sortOrder: z.number().int().nonnegative(),
    active: z.boolean(),
  })
  .strict();

export type VocabularyRow = z.infer<typeof vocabularyRowSchema>;

export function normalizeSourceToken(value: string) {
  return value
    .normalize("NFKC")
    .trim()
    .replace(/\s+/g, " ")
    .toLocaleLowerCase("ru-RU");
}

function scenario(
  slug: string,
  labelEn: string,
  sourceAliases: string[],
  sortOrder: number,
): VocabularyRow {
  return {
    kind: "wearing_scenario",
    slug,
    labelEn,
    sourceAliases,
    sortOrder,
    active: true,
  };
}

function complication(
  slug: string,
  labelEn: string,
  sourceAliases: string[],
  sortOrder: number,
): VocabularyRow {
  return {
    kind: "complication",
    slug,
    labelEn,
    sourceAliases,
    sortOrder,
    active: true,
  };
}

function positioning(
  slug: string,
  labelEn: string,
  sortOrder: number,
): VocabularyRow {
  return {
    kind: "positioning_group",
    slug,
    labelEn,
    sourceAliases: [],
    sortOrder,
    active: true,
  };
}

export const BUNDLED_VOCABULARY: readonly VocabularyRow[] = [
  scenario("everyday", "Everyday", ["повседневно", "везде", "casual"], 10),
  scenario("office", "Office", ["офис"], 20),
  scenario("business", "Business", ["бизнес", "международный бизнес"], 30),
  scenario("boardroom", "Boardroom", ["совет директоров"], 40),
  scenario("negotiation", "Negotiation", ["переговоры", "переговорные"], 50),
  scenario("executive", "Executive", ["представительский"], 60),
  scenario("smart_casual", "Smart casual", ["smart casual"], 70),
  scenario("sport_chic", "Sport chic", ["спорт-шик"], 80),
  scenario("suit", "Suit", ["костюм", "строгий костюм"], 90),
  scenario("black_tie", "Black tie", ["дресс-код", "смокинг"], 100),
  scenario("evening", "Evening", [
    "вечер",
    "вечерний прием",
    "вечерний выход",
    "вечерний раут",
  ], 110),
  scenario("gala", "Gala", ["гала"], 120),
  scenario("reception", "Reception", [
    "раут",
    "приемы",
    "официальные приемы",
    "официальные встречи",
  ], 130),
  scenario("high_society", "High society", ["высший свет"], 140),
  scenario("club", "Club", ["клуб"], 150),
  scenario("private_club", "Private club", ["закрытый клуб"], 160),
  scenario("cocktail", "Cocktail", ["коктейль"], 170),
  scenario("theatre", "Theatre", ["театр"], 180),
  scenario("status", "Status", ["статус", "топ-статус"], 190),
  scenario("collection", "Collection", [
    "коллекция",
    "музейная коллекция",
  ], 200),
  scenario("auction", "Auction", ["аукцион", "аукционы"], 210),
  scenario("art", "Art", ["арт"], 220),
  scenario("weekend", "Weekend", ["уикенд"], 230),
  scenario("leisure", "Leisure", ["отдых"], 240),
  scenario("sport", "Sport", ["спорт", "повседневный спорт"], 250),
  scenario("motorsport", "Motorsport", ["автоспорт", "гоночный"], 260),
  scenario("field", "Field", ["полевые"], 270),
  scenario("expedition", "Expedition", ["экспедиции"], 280),
  scenario("caving", "Caving", ["спелеология"], 290),
  scenario("extreme", "Extreme", ["экстрим"], 300),
  scenario("diving", "Diving", ["дайвинг"], 310),
  scenario("professional_diving", "Professional diving", [
    "профессиональный дайвинг",
  ], 320),
  scenario("deep_sea_diving", "Deep-sea diving", [
    "глубоководный дайвинг",
    "экстремальный дайвинг",
  ], 330),
  scenario("marine_sport", "Marine sport", ["морской спорт"], 340),
  scenario("sailing", "Sailing", ["парусный спорт", "парусные гонки"], 350),
  scenario("regatta", "Regatta", ["регата"], 360),
  scenario("yachting", "Yachting", ["яхтинг", "яхта"], 370),
  scenario("beach", "Beach", ["пляж"], 380),
  scenario("resort", "Resort", ["курорт"], 390),
  scenario("travel", "Travel", ["путешествия"], 400),
  scenario("business_travel", "Business travel", [
    "бизнес-тревел",
    "бизнес-авиация",
  ], 410),
  scenario("flights", "Flights", ["перелеты"], 420),
  scenario("aviation", "Aviation", ["авиация"], 430),
  scenario("laboratory", "Laboratory", ["наука", "лаборатории"], 440),

  complication("time_only", "Time only", ["нет (time-only)"], 10),
  complication("date", "Date", ["дата", "мгновенная дата"], 20),
  complication("pointer_date", "Pointer date", [
    "стрелочная дата",
    "стрелочный указатель даты",
    "стрелочный календарь",
  ], 30),
  complication("day_of_week", "Day of week", ["день недели"], 40),
  complication("annual_calendar", "Annual calendar", ["годовой календарь"], 50),
  complication("moonphase", "Moon phase", ["фазы луны"], 60),
  complication("small_seconds", "Small seconds", [
    "малая секундная стрелка",
  ], 70),
  complication("gmt", "GMT / second time zone", [
    "второй пояс gmt",
    "второй пояс",
    "dual time 24h",
  ], 80),
  complication("day_night_indicator", "Day/night indicator", [
    "день/ночь",
  ], 90),
  complication("bezel_24h", "24-hour bezel", ["безель 24ч"], 100),
  complication("dive_bezel", "Dive bezel", [
    "дайверский безель",
    "дайверский безель 60 мин",
  ], 110),
  complication("sixty_minute_bezel", "60-minute bezel", ["безель 60 мин"], 120),
  complication("chronograph", "Chronograph", ["хронограф"], 130),
  complication("tachymeter", "Tachymeter", ["тахиметр"], 140),
  complication("flyback", "Flyback", ["flyback"], 150),
  complication("regatta_timer", "Regatta timer", [
    "программируемый таймер регаты",
  ], 160),
  complication("helium_valve", "Helium escape valve", [
    "гелиевый клапан",
  ], 170),
  complication("antimagnetic_shield", "Antimagnetic shield", [
    "антимагнитный экран 1000g",
  ], 180),
  complication("ring_command", "Ring Command bezel", ["ring command"], 190),
  complication("ringlock", "Ringlock case system", ["ringlock"], 200),

  positioning("instrument", "Instrument", 10),
  positioning("quiet_classic", "Quiet classic", 20),
  positioning("recognised_benchmark", "Recognised benchmark", 30),
  positioning("bicolour", "Two-tone", 40),
  positioning("precious_metal", "Solid precious metal", 50),
  positioning("platinum_ice", "Platinum / Ice Blue", 60),
  positioning("high_jewellery", "High jewellery", 70),
  positioning("avant_garde", "Avant-garde", 80),
  positioning("sport_luxe", "Sport-luxe", 90),
  positioning("expressive_dial", "Expressive dial", 100),
  positioning("mechanical_showcase", "Mechanical showcase", 110),
];

export function resolveVocabularySlug(
  kind: VocabularyKind,
  token: string,
  rows: readonly VocabularyRow[] = BUNDLED_VOCABULARY,
) {
  const normalized = normalizeSourceToken(token);
  for (const row of rows) {
    if (row.kind !== kind) continue;
    if (row.sourceAliases.some((alias) => normalizeSourceToken(alias) === normalized)) {
      return row.slug;
    }
  }
  return null;
}

function splitOn(cell: string, separator: RegExp) {
  return cell
    .split(separator)
    .map((token) => token.trim())
    .filter((token) => token.length > 0);
}

export function splitScenarioTokens(cell: string) {
  return splitOn(cell, /\s*\/\s*/);
}

export function splitComplicationTokens(cell: string) {
  return splitOn(cell, /\s*,\s*/);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/domain/catalogue-vocabulary.test.ts`
Expected: PASS, 13 tests.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/domain/catalogue-vocabulary.ts app/domain/catalogue-vocabulary.test.ts
git commit -m "$(cat <<'MSG'
add catalogue vocabulary domain module

Introduces the wearing-scenario, complication, and positioning-group
vocabularies with Russian source aliases for deterministic sheet intake.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 2: Positioning phrase-to-group mapping

**Files:**

- Create: `data/catalogue/positioning-map.json`
- Modify: `app/domain/catalogue-vocabulary.ts`
- Modify: `app/domain/catalogue-vocabulary.test.ts`

**Interfaces:**

- Consumes: `normalizeSourceToken`, `VocabularyRow`, `BUNDLED_VOCABULARY` from Task 1.
- Produces: `resolvePositioningGroup(phrase: string): string | null` — returns a `positioning_group` slug or `null` when the phrase is not in the committed map.

The mapping lives in JSON rather than code so the owner can correct any assignment without a code change, per spec section 4.

- [ ] **Step 1: Write the failing test**

Append to `app/domain/catalogue-vocabulary.test.ts`:

```typescript
import { resolvePositioningGroup } from "./catalogue-vocabulary";

describe("resolvePositioningGroup", () => {
  it("maps an avant-garde phrase", () => {
    expect(
      resolvePositioningGroup("авангардный флагман с открытым калибром"),
    ).toBe("avant_garde");
  });

  it("maps a neoclassical phrase", () => {
    expect(resolvePositioningGroup("современная неоклассика")).toBe(
      "quiet_classic",
    );
  });

  it("maps a platinum Ice Blue phrase", () => {
    expect(resolvePositioningGroup("ультимативная платина Ice Blue")).toBe(
      "platinum_ice",
    );
  });

  it("maps an instrument phrase", () => {
    expect(resolvePositioningGroup("эталонный инструмент")).toBe("instrument");
  });

  it("is whitespace and case insensitive", () => {
    expect(resolvePositioningGroup("  Тихая   Классика ")).toBe(
      "quiet_classic",
    );
  });

  it("returns null for an unmapped phrase", () => {
    expect(resolvePositioningGroup("никогда не встречалось")).toBeNull();
  });

  it("only ever returns a known positioning group slug", () => {
    const groups = new Set(
      BUNDLED_VOCABULARY.filter(
        (row) => row.kind === "positioning_group",
      ).map((row) => row.slug),
    );
    const mapped = Object.values(
      positioningMapForTest,
    ) as string[];
    for (const slug of mapped) expect(groups.has(slug)).toBe(true);
  });
});
```

Add `positioningMapForTest` to the import list from `./catalogue-vocabulary`.

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/catalogue-vocabulary.test.ts`
Expected: FAIL — `resolvePositioningGroup` is not exported.

- [ ] **Step 3: Create the mapping data**

Create `data/catalogue/positioning-map.json`. Keys are the owner's phrases exactly as written in the sheet; values are `positioning_group` slugs. Populate it from the owner's sample using these group assignments:

- `instrument` — эталонный инструмент; канонический стандарт; мировой эталон дайвера; инструментальный полярный тул; тяжелый глубоководный инструмент; инженерный предел прочности; мировой рекорд глубины; чистая утилитарность; оригинальный инструментальный вес; прагматичный аутдор-люкс; инструментальный милитари с гардами; мировой авиационный бенчмарк; зеленое стекло / 1000 Gauss; программируемый таймер регаты; механический хронометр регаты
- `quiet_classic` — современная неоклассика; тихая классика; строгая современная классика; золотая середина часового стиля; тихая монохромная роскошь; академическая костюмная сдержанность; сдержанная женственность; минимализм / классика; серебряный классический циферблат; сбалансированная база; аккуратный традиционный размер; чистота линий и прозрачное дно; элегантная женская база; бизнес-стандарт; универсальный современный размер; базовый современный люкс
- `recognised_benchmark` — эталонный статус; визитная карточка Rolex; узнаваемый символ успеха; исторический статус лидера; флагманский статус руководителя; максимальная мировая ликвидность; золотой фундаментальный статус; абсолютная золотая классика; культовая золотая классика; культовый тревел-хронометр; культовая коллекционная модель; хайп и коллекционный статус; элегантный высокий спрос; узнаваемый яркий статус; узнаваемая женская икона
- `bicolour` — неовинтажный биколор; гоночный биколор; контрастный строгий биколор; премиальный яркий биколор; теплая биколорная эстетика; нежный биколор; компактная биколорная классика; сдержанный биколор; динамичный биколор; базовая биколорная элегантность; биколорный аккуратный размер; биколорный классический стиль; биколорный 41-мм формат; мощный биколорный инструмент; мощная биколорная механика; утонченный биколор; элегантный теплый биколор; неовинтажный баланс массы
- `precious_metal` — массивное золото Everose; тяжелый цельнозолотой дайвер; тяжелый цельнозолотой люкс; тяжелое белое золото Destro; тяжелый цельный Everose; миниатюрный золотой престиж; монохромное золото; сияющий золотой корпус; компактный золотой камень; богатый оттенок Everose; массивный тихий люкс; монументальная золотая классика; массивный золотой тревел; монументальный теплый люкс; ультратяжелый цельнозолотой дайвер; экстравертный цельнозолотой спорт; тяжелый золотой регата-хронограф; выразительный золотой контраст; контрастный теплый люкс; бескомпромиссная роскошь; тихий суверенный престиж
- `platinum_ice` — ультимативная платина Ice Blue; флагманская платина с display-back; платиновый ювелирный эксклюзив; платиновый ледяной Ice Blue; тихий платиновый авторитет; платиновый рифленый безель; редчайшая платина TBR; платиновый гильошированный шедевр; прозрачная крышка / платина; сталь с платиновым безелем 950
- `high_jewellery` — высокий ювелирный багетный декор; монументальное ювелирное искусство; высокий ювелирный спорт; ледяное бриллиантовое сияние; высокое ювелирное искусство; драгоценный хронометр; ультимативный ювелирный statement; цельнозолотой ювелирный шик; холодный бриллиантовый блеск; игривый безель с камнями; изящный ювелирный акцент; сдержанные бриллианты; роскошное теплое паве; чистый бриллиантовый свет; ювелирный шик; бриллиантовый холодный блеск; драгоценный миниатюрный шедевр; традиционное ювелирное золото; высокий ювелирный свет; бриллиантовый безель; ультимативное ювелирное паве; бриллиантовый президент; яркий ювелирный President; холодная ювелирная эстетика; багетная огранка камней; монохромный ювелирный люкс; массивный ювелирный президент; бриллиантовый 40-мм безель; элегантный драгоценный монохром; ювелирный безель; бриллианты и желтое золото; драгоценный сложный люкс; экстравагантный золотой шик; классическая вечерняя роскошь; нарядный классический блеск; яркое сияние и классика; холодный чистый блеск; эксклюзивный драгоценный люкс; браслет Pearlmaster с бриллиантами; эксклюзивный драгоценный камень; ювелирная классика
- `avant_garde` — авангардный флагман с открытым калибром; высокая классика с открытым балансом; апогей модельного ряда Land-Dweller; апогей модельного ряда Day-Date; леворучный инсайдер Sprite; неординарный дизайн; редкий минеральный циферблат
- `sport_luxe` — элегантный каучуковый хронограф; строгий монохром на каучуке; скрытая роскошь на каучуке; сложный роскошный спорт-шик; компактный спорт-люкс; статусный морской спорт; золото с матовым безелем Cerachrom; динамичный золотой гранд-тревел; роскошный хронограф Everose; контрастный гоночный дух; сверхлегкий титан (~100 г)
- `expressive_dial` — пастельный мягкий тон; сдержанный бежевый оттенок; пастельный баланс; модный фисташковый акцент; контрастный циферблат; яркий коллекционный поп-арт; градиентный циферблат; сбалансированный серый Cerachrom; благородный медный оттенок; рифленый теплый безель; рифленое розовое золото; утонченный рифленый безель; нежная теплая классика
- `mechanical_showcase` — годовой календарь Saros; диск фаз луны из метеорита; индикатор день/ночь и второй пояс; стрелочный календарь

Shape:

```json
{
  "авангардный флагман с открытым калибром": "avant_garde",
  "современная неоклассика": "quiet_classic",
  "эталонный инструмент": "instrument"
}
```

- [ ] **Step 4: Write the implementation**

Append to `app/domain/catalogue-vocabulary.ts`:

```typescript
import rawPositioningMap from "../../data/catalogue/positioning-map.json";

const positioningMapSchema = z.record(z.string().min(1), slugSchema);

export const positioningMapForTest = positioningMapSchema.parse(
  rawPositioningMap,
);

const positioningByNormalizedPhrase = new Map(
  Object.entries(positioningMapForTest).map(([phrase, slug]) => [
    normalizeSourceToken(phrase),
    slug,
  ]),
);

export function resolvePositioningGroup(phrase: string) {
  return positioningByNormalizedPhrase.get(normalizeSourceToken(phrase)) ?? null;
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run app/domain/catalogue-vocabulary.test.ts`
Expected: PASS, 20 tests.

- [ ] **Step 6: Run the full gate and commit**

```bash
npm run check
git add data/catalogue/positioning-map.json app/domain/catalogue-vocabulary.ts app/domain/catalogue-vocabulary.test.ts
git commit -m "$(cat <<'MSG'
add reviewable positioning phrase-to-group mapping

Assigns the owner's positioning phrases to eleven facet groups in
committed JSON so any assignment can be corrected without a code change.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 3: Sheet row parser

**Files:**

- Create: `app/domain/sheet-intake.ts`
- Test: `app/domain/sheet-intake.test.ts`

**Interfaces:**

- Consumes: `resolveVocabularySlug`, `resolvePositioningGroup`, `splitScenarioTokens`, `splitComplicationTokens` from Tasks 1-2.
- Produces:
  - `SHEET_COLUMNS: readonly string[]` — the nineteen expected headers in order
  - `microAdjustmentSchema` — `{ present: boolean, systemName: string | null, rangeMm: number | null }`
  - `parseMicroAdjustment(cell: string): MicroAdjustment`
  - `parseWaterResistanceM(cell: string): number | null`
  - `parseCaseShape(cell: string): { shape: CaseShape | null; integratedHint: boolean }`
  - `parseMovementConstruction(cell: string): { construction: "mass_produced" | "manufacture" | null; caliber: string | null }`
  - `parsePriceUsdMinor(cell: string): number | null`
  - `parseAllergyRisk(cell: string): "none_known" | "possible" | null`
  - `parseYesNo(cell: string): boolean | null`
  - `sheetRowSchema`, `type SheetRow`
  - `parseSheetRow(cells: string[], sourceLine: number): { ok: true; row: SheetRow } | { ok: false; reasons: string[] }`

- [ ] **Step 1: Write the failing test**

Create `app/domain/sheet-intake.test.ts`:

```typescript
import { describe, expect, it } from "vitest";

import {
  parseAllergyRisk,
  parseCaseShape,
  parseMicroAdjustment,
  parseMovementConstruction,
  parsePriceUsdMinor,
  parseSheetRow,
  parseWaterResistanceM,
  parseYesNo,
} from "./sheet-intake";

describe("parseMicroAdjustment", () => {
  it("parses a present system with a range", () => {
    expect(parseMicroAdjustment("Easylink (+5 мм)")).toEqual({
      present: true,
      systemName: "Easylink",
      rangeMm: 5,
    });
  });

  it("parses a range expressed as an upper bound", () => {
    expect(parseMicroAdjustment("Glidelock (до 20 мм)")).toEqual({
      present: true,
      systemName: "Glidelock",
      rangeMm: 20,
    });
  });

  it("parses a compound system with no numeric range", () => {
    expect(parseMicroAdjustment("Glidelock + Fliplock")).toEqual({
      present: true,
      systemName: "Glidelock + Fliplock",
      rangeMm: null,
    });
  });

  it("parses an absent system and keeps its clasp name", () => {
    expect(parseMicroAdjustment("нет (Crownclasp)")).toEqual({
      present: false,
      systemName: "Crownclasp",
      rangeMm: null,
    });
  });

  it("parses a bare absence", () => {
    expect(parseMicroAdjustment("нет (пряжка)")).toEqual({
      present: false,
      systemName: "пряжка",
      rangeMm: null,
    });
  });
});

describe("parseWaterResistanceM", () => {
  it("parses a metric depth", () => {
    expect(parseWaterResistanceM("100 м")).toBe(100);
    expect(parseWaterResistanceM("1220 м")).toBe(1220);
  });

  it("returns null for an unparseable cell", () => {
    expect(parseWaterResistanceM("")).toBeNull();
    expect(parseWaterResistanceM("нет данных")).toBeNull();
  });
});

describe("parseCaseShape", () => {
  it("parses a plain round case", () => {
    expect(parseCaseShape("круглая")).toEqual({
      shape: "round",
      integratedHint: false,
    });
  });

  it("parses a round case with an integrated marker", () => {
    expect(parseCaseShape("круглая (интегрир.)")).toEqual({
      shape: "round",
      integratedHint: true,
    });
  });

  it("returns a null shape for an unknown value", () => {
    expect(parseCaseShape("неизвестная")).toEqual({
      shape: null,
      integratedHint: false,
    });
  });
});

describe("parseMovementConstruction", () => {
  it("splits construction from the caliber", () => {
    expect(parseMovementConstruction("мануфактурный (Cal. 7135)")).toEqual({
      construction: "manufacture",
      caliber: "Cal. 7135",
    });
  });

  it("parses construction with no caliber", () => {
    expect(parseMovementConstruction("мануфактурный")).toEqual({
      construction: "manufacture",
      caliber: null,
    });
  });

  it("parses mass-produced construction", () => {
    expect(parseMovementConstruction("массовый (ETA 2824-2)")).toEqual({
      construction: "mass_produced",
      caliber: "ETA 2824-2",
    });
  });

  it("returns nulls for an unrecognised cell", () => {
    expect(parseMovementConstruction("")).toEqual({
      construction: null,
      caliber: null,
    });
  });
});

describe("parsePriceUsdMinor", () => {
  it("parses a formatted dollar amount into minor units", () => {
    expect(parsePriceUsdMinor("$16,500")).toBe(1_650_000);
  });

  it("parses an amount with no separators", () => {
    expect(parsePriceUsdMinor("$450000")).toBe(45_000_000);
  });

  it("returns null for an empty cell", () => {
    expect(parsePriceUsdMinor("")).toBeNull();
  });
});

describe("parseAllergyRisk", () => {
  it("maps TRUE to a possible nickel contact risk", () => {
    expect(parseAllergyRisk("TRUE")).toBe("possible");
  });

  it("maps FALSE to no known risk", () => {
    expect(parseAllergyRisk("FALSE")).toBe("none_known");
  });

  it("returns null when the cell is blank", () => {
    expect(parseAllergyRisk("")).toBeNull();
  });
});

describe("parseYesNo", () => {
  it("parses Russian affirmatives and negatives", () => {
    expect(parseYesNo("да")).toBe(true);
    expect(parseYesNo("нет")).toBe(false);
    expect(parseYesNo("")).toBeNull();
  });
});

const LAND_DWELLER = [
  "Rolex Land-Dweller 40 White Rolesor M127334-0001",
  "https://www.rolex.com/watches/land-dweller",
  "47.4",
  "10.5",
  "40",
  "круглая (интегрир.)",
  "да",
  "да",
  "automatic",
  "мануфактурный (Cal. 7135)",
  "100 м",
  "сапфир",
  "21",
  "Easylink (+5 мм)",
  "офис / спорт-шик / smart casual",
  "авангардный флагман с открытым калибром",
  "Дата",
  "$16,500",
  "TRUE",
];

describe("parseSheetRow", () => {
  it("parses a complete row", () => {
    const result = parseSheetRow(LAND_DWELLER, 2);
    expect(result.ok).toBe(true);
    if (!result.ok) return;
    expect(result.row.referenceCode).toBe("127334-0001");
    expect(result.row.caseDiameterMm).toBe(40);
    expect(result.row.caseShape).toBe("round");
    expect(result.row.integratedBracelet).toBe(true);
    expect(result.row.displayCaseback).toBe(true);
    expect(result.row.movementType).toBe("automatic");
    expect(result.row.movementConstruction).toBe("manufacture");
    expect(result.row.caliber).toBe("Cal. 7135");
    expect(result.row.waterResistanceM).toBe(100);
    expect(result.row.crystal).toBe("sapphire");
    expect(result.row.microAdjustment.present).toBe(true);
    expect(result.row.wearingScenarios).toEqual([
      "office",
      "sport_chic",
      "smart_casual",
    ]);
    expect(result.row.complications).toEqual(["date"]);
    expect(result.row.positioningGroup).toBe("avant_garde");
    expect(result.row.positioningLine).toBe(
      "авангардный флагман с открытым калибром",
    );
    expect(result.row.priceUsdMinor).toBe(1_650_000);
    expect(result.row.nickelContactRisk).toBe("possible");
  });

  it("rejects a row with the wrong column count", () => {
    const result = parseSheetRow(LAND_DWELLER.slice(0, 5), 3);
    expect(result.ok).toBe(false);
    if (result.ok) return;
    expect(result.reasons[0]).toContain("19 columns");
  });

  it("rejects a row with an unmapped scenario token", () => {
    const cells = [...LAND_DWELLER];
    cells[14] = "офис / марсоход";
    const result = parseSheetRow(cells, 4);
    expect(result.ok).toBe(false);
    if (result.ok) return;
    expect(result.reasons.join(" ")).toContain("марсоход");
  });

  it("rejects a row with an unmapped complication token", () => {
    const cells = [...LAND_DWELLER];
    cells[16] = "Дата, вечный двигатель";
    const result = parseSheetRow(cells, 5);
    expect(result.ok).toBe(false);
    if (result.ok) return;
    expect(result.reasons.join(" ")).toContain("вечный двигатель");
  });

  it("accepts an unmapped positioning phrase but leaves the group null", () => {
    const cells = [...LAND_DWELLER];
    cells[15] = "совершенно новая формулировка";
    const result = parseSheetRow(cells, 6);
    expect(result.ok).toBe(true);
    if (!result.ok) return;
    expect(result.row.positioningGroup).toBeNull();
    expect(result.row.positioningLine).toBe("совершенно новая формулировка");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/sheet-intake.test.ts`
Expected: FAIL — `Failed to resolve import "./sheet-intake"`.

- [ ] **Step 3: Write the implementation**

Create `app/domain/sheet-intake.ts`:

```typescript
import { z } from "zod";

import {
  resolvePositioningGroup,
  resolveVocabularySlug,
  splitComplicationTokens,
  splitScenarioTokens,
} from "./catalogue-vocabulary";

export const SHEET_COLUMNS = [
  "Модель / reference",
  "Ссылка производителя",
  "lug2lug, mm",
  "Толщина корпуса, mm",
  "Диаметр корпуса, mm",
  "Форма корпуса",
  "Дно: display-back",
  "Интегрированный браслет",
  "Механизм: тип",
  "Механизм: массовый / мануфактурный (Калибр)",
  "Водозащита",
  "Стекло",
  "Ширина крепления (lug width), mm",
  "Микрорегулировка",
  "Сценарий ношения",
  "Социальный контекст",
  "Complications (Усложнения)",
  "Цена (ориентир Август 2026, USD)",
  "Allergic (риск аллергии)",
] as const;

export const CASE_SHAPES = [
  "round",
  "tonneau",
  "rectangular",
  "cushion",
  "square",
  "oval",
  "other",
] as const;
export type CaseShape = (typeof CASE_SHAPES)[number];

export const microAdjustmentSchema = z
  .object({
    present: z.boolean(),
    systemName: z.string().trim().min(1).max(80).nullable(),
    rangeMm: z.number().positive().max(50).nullable(),
  })
  .strict();
export type MicroAdjustment = z.infer<typeof microAdjustmentSchema>;

const CRYSTALS: Record<string, "sapphire" | "mineral" | "acrylic" | "other"> = {
  сапфир: "sapphire",
  "зеленый сапфир": "sapphire",
  минеральное: "mineral",
  акрил: "acrylic",
  гесалит: "acrylic",
};

const SHAPES: Record<string, CaseShape> = {
  круглая: "round",
  тонно: "tonneau",
  прямоугольная: "rectangular",
  подушка: "cushion",
  квадратная: "square",
  овальная: "oval",
};

const MOVEMENT_TYPES = [
  "automatic",
  "manual",
  "quartz",
  "solar",
  "spring_drive",
  "hybrid",
] as const;

function lower(value: string) {
  return value.normalize("NFKC").trim().toLocaleLowerCase("ru-RU");
}

export function parseMicroAdjustment(cell: string): MicroAdjustment {
  const text = cell.trim();
  if (text.length === 0) {
    return { present: false, systemName: null, rangeMm: null };
  }
  const parenthetical = text.match(/^(.*?)\s*\(([^)]*)\)\s*$/);
  const head = (parenthetical?.[1] ?? text).trim();
  const inside = parenthetical?.[2]?.trim() ?? null;
  const absent = lower(head).startsWith("нет");

  if (absent) {
    return { present: false, systemName: inside || null, rangeMm: null };
  }
  const range = inside?.match(/(\d+(?:[.,]\d+)?)/);
  return {
    present: true,
    systemName: head || null,
    rangeMm: range ? Number(range[1]!.replace(",", ".")) : null,
  };
}

export function parseWaterResistanceM(cell: string) {
  const match = cell.match(/(\d+)/);
  return match ? Number(match[1]) : null;
}

export function parseCaseShape(cell: string) {
  const text = lower(cell);
  const integratedHint = /интегрир/.test(text);
  const base = text.replace(/\s*\([^)]*\)\s*/g, "").trim();
  return { shape: SHAPES[base] ?? null, integratedHint };
}

export function parseMovementConstruction(cell: string) {
  const text = cell.trim();
  if (text.length === 0) return { construction: null, caliber: null };
  const parenthetical = text.match(/^(.*?)\s*\(([^)]*)\)\s*$/);
  const head = lower(parenthetical?.[1] ?? text);
  const caliber = parenthetical?.[2]?.trim() || null;
  const construction = head.startsWith("мануфактур")
    ? ("manufacture" as const)
    : head.startsWith("массов")
      ? ("mass_produced" as const)
      : null;
  return { construction, caliber };
}

export function parsePriceUsdMinor(cell: string) {
  const digits = cell.replace(/[^\d.]/g, "");
  if (digits.length === 0) return null;
  const amount = Number(digits);
  return Number.isFinite(amount) && amount > 0 ? Math.round(amount * 100) : null;
}

export function parseAllergyRisk(cell: string) {
  const text = lower(cell);
  if (text === "true") return "possible" as const;
  if (text === "false") return "none_known" as const;
  return null;
}

export function parseYesNo(cell: string) {
  const text = lower(cell);
  if (text === "да" || text === "yes" || text === "true") return true;
  if (text === "нет" || text === "no" || text === "false") return false;
  return null;
}

function parseNumber(cell: string) {
  const match = cell.replace(",", ".").match(/(\d+(?:\.\d+)?)/);
  return match ? Number(match[1]) : null;
}

function parseReferenceCode(identity: string) {
  const tokens = identity.trim().split(/\s+/);
  const candidates = tokens.filter((token) =>
    /^M?\d{5,6}[A-Z]*(?:-\d{4})?$/i.test(token),
  );
  const chosen = candidates.at(-1) ?? null;
  return chosen ? chosen.replace(/^M/i, "") : null;
}

export const sheetRowSchema = z
  .object({
    sourceLine: z.number().int().positive(),
    identity: z.string().trim().min(1).max(300),
    referenceCode: z.string().trim().min(1).max(60),
    productUrl: z.url(),
    lugToLugMm: z.number().positive().nullable(),
    caseThicknessMm: z.number().positive().nullable(),
    caseDiameterMm: z.number().positive().nullable(),
    caseShape: z.enum(CASE_SHAPES).nullable(),
    displayCaseback: z.boolean().nullable(),
    integratedBracelet: z.boolean().nullable(),
    movementType: z.enum(MOVEMENT_TYPES),
    movementConstruction: z.enum(["mass_produced", "manufacture"]).nullable(),
    caliber: z.string().trim().min(1).max(80).nullable(),
    waterResistanceM: z.number().nonnegative().nullable(),
    crystal: z.enum(["sapphire", "mineral", "acrylic", "other"]).nullable(),
    lugWidthMm: z.number().positive().nullable(),
    microAdjustment: microAdjustmentSchema,
    wearingScenarios: z.array(z.string().min(1)).min(1),
    positioningLine: z.string().trim().min(1).max(300).nullable(),
    positioningGroup: z.string().min(1).nullable(),
    complications: z.array(z.string().min(1)),
    priceUsdMinor: z.number().int().positive().nullable(),
    nickelContactRisk: z.enum(["none_known", "possible"]).nullable(),
  })
  .strict();

export type SheetRow = z.infer<typeof sheetRowSchema>;

export function parseSheetRow(
  cells: string[],
  sourceLine: number,
): { ok: true; row: SheetRow } | { ok: false; reasons: string[] } {
  const reasons: string[] = [];
  if (cells.length !== SHEET_COLUMNS.length) {
    return {
      ok: false,
      reasons: [
        `Line ${sourceLine}: expected 19 columns, received ${cells.length}.`,
      ],
    };
  }

  const at = (index: number) => (cells[index] ?? "").trim();
  const identity = at(0);
  const referenceCode = parseReferenceCode(identity);
  if (!referenceCode) {
    reasons.push(`Line ${sourceLine}: no reference code in "${identity}".`);
  }

  const movementType = lower(at(8));
  if (!MOVEMENT_TYPES.includes(movementType as (typeof MOVEMENT_TYPES)[number])) {
    reasons.push(`Line ${sourceLine}: unknown movement type "${at(8)}".`);
  }

  const shape = parseCaseShape(at(5));
  const construction = parseMovementConstruction(at(9));

  const wearingScenarios: string[] = [];
  for (const token of splitScenarioTokens(at(14))) {
    const slug = resolveVocabularySlug("wearing_scenario", token);
    if (slug === null) {
      reasons.push(`Line ${sourceLine}: unmapped wearing scenario "${token}".`);
      continue;
    }
    if (!wearingScenarios.includes(slug)) wearingScenarios.push(slug);
  }
  if (wearingScenarios.length === 0) {
    reasons.push(`Line ${sourceLine}: no wearing scenario resolved.`);
  }

  const complications: string[] = [];
  for (const token of splitComplicationTokens(at(16))) {
    const slug = resolveVocabularySlug("complication", token);
    if (slug === null) {
      reasons.push(`Line ${sourceLine}: unmapped complication "${token}".`);
      continue;
    }
    if (!complications.includes(slug)) complications.push(slug);
  }

  if (reasons.length > 0) return { ok: false, reasons };

  const positioningLine = at(15) || null;
  const parsed = sheetRowSchema.safeParse({
    sourceLine,
    identity,
    referenceCode,
    productUrl: at(1),
    lugToLugMm: parseNumber(at(2)),
    caseThicknessMm: parseNumber(at(3)),
    caseDiameterMm: parseNumber(at(4)),
    caseShape: shape.shape,
    displayCaseback: parseYesNo(at(6)),
    integratedBracelet: parseYesNo(at(7)) ?? shape.integratedHint,
    movementType,
    movementConstruction: construction.construction,
    caliber: construction.caliber,
    waterResistanceM: parseWaterResistanceM(at(10)),
    crystal: CRYSTALS[lower(at(11))] ?? null,
    lugWidthMm: parseNumber(at(12)),
    microAdjustment: parseMicroAdjustment(at(13)),
    wearingScenarios,
    positioningLine,
    positioningGroup: positioningLine
      ? resolvePositioningGroup(positioningLine)
      : null,
    complications,
    priceUsdMinor: parsePriceUsdMinor(at(17)),
    nickelContactRisk: parseAllergyRisk(at(18)),
  });

  return parsed.success
    ? { ok: true, row: parsed.data }
    : {
        ok: false,
        reasons: parsed.error.issues.map(
          (issue) =>
            `Line ${sourceLine}: ${issue.path.join(".")} ${issue.message}`,
        ),
      };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/domain/sheet-intake.test.ts`
Expected: PASS, 24 tests.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/domain/sheet-intake.ts app/domain/sheet-intake.test.ts
git commit -m "$(cat <<'MSG'
add model sheet row parser

Parses the nineteen intake columns, including structured
micro-adjustment, construction with caliber, and allergy mapping.
Unmapped vocabulary tokens reject the row instead of being dropped.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 4: Sheet deduplication

**Files:**

- Create: `app/domain/sheet-dedup.ts`
- Test: `app/domain/sheet-dedup.test.ts`

**Interfaces:**

- Consumes: `SheetRow` from Task 3.
- Produces:
  - `isWorkbookArtifact(cells: string[]): boolean`
  - `dedupeSheetRows(rows: SheetRow[]): { accepted: SheetRow[]; collapsed: { referenceCode: string; sourceLines: number[] }[]; conflicts: { referenceCode: string; sourceLines: number[]; fields: string[] }[] }`

Spec section 8: exact duplicates collapse and record their source lines; same-reference-different-values conflicts are rejected for the owner, never auto-resolved.

- [ ] **Step 1: Write the failing test**

Create `app/domain/sheet-dedup.test.ts`:

```typescript
import { describe, expect, it } from "vitest";

import { dedupeSheetRows, isWorkbookArtifact } from "./sheet-dedup";
import type { SheetRow } from "./sheet-intake";

function row(overrides: Partial<SheetRow> & { sourceLine: number }): SheetRow {
  return {
    identity: "Rolex Submariner Date 126610LN Oystersteel",
    referenceCode: "126610LN",
    productUrl: "https://www.rolex.com/en-us/watches/submariner",
    lugToLugMm: 47.6,
    caseThicknessMm: 12.5,
    caseDiameterMm: 41,
    caseShape: "round",
    displayCaseback: false,
    integratedBracelet: false,
    movementType: "automatic",
    movementConstruction: "manufacture",
    caliber: "Cal. 3235",
    waterResistanceM: 300,
    crystal: "sapphire",
    lugWidthMm: 21,
    microAdjustment: { present: true, systemName: "Glidelock", rangeMm: 20 },
    wearingScenarios: ["sport", "office"],
    positioningLine: "мировой эталон дайвера",
    positioningGroup: "instrument",
    complications: ["date", "dive_bezel"],
    priceUsdMinor: 1_440_000,
    nickelContactRisk: "possible",
    ...overrides,
  };
}

describe("isWorkbookArtifact", () => {
  it("detects a needs-research complications cell", () => {
    const cells = new Array(19).fill("");
    cells[16] = "needs research";
    expect(isWorkbookArtifact(cells)).toBe(true);
  });

  it("detects the seed-catalogue placeholder note", () => {
    const cells = new Array(19).fill("");
    cells[18] =
      "Нет точного варианта в seed-catalogue; заполнить по первичному источнику";
    expect(isWorkbookArtifact(cells)).toBe(true);
  });

  it("passes a normal row through", () => {
    const cells = new Array(19).fill("x");
    expect(isWorkbookArtifact(cells)).toBe(false);
  });
});

describe("dedupeSheetRows", () => {
  it("keeps a single row untouched", () => {
    const result = dedupeSheetRows([row({ sourceLine: 2 })]);
    expect(result.accepted).toHaveLength(1);
    expect(result.collapsed).toHaveLength(0);
    expect(result.conflicts).toHaveLength(0);
  });

  it("collapses exact duplicates and records their lines", () => {
    const result = dedupeSheetRows([
      row({ sourceLine: 2 }),
      row({ sourceLine: 40 }),
    ]);
    expect(result.accepted).toHaveLength(1);
    expect(result.collapsed).toEqual([
      { referenceCode: "126610LN", sourceLines: [2, 40] },
    ]);
    expect(result.conflicts).toHaveLength(0);
  });

  it("collapses duplicates that differ only in the identity label", () => {
    const result = dedupeSheetRows([
      row({ sourceLine: 2, identity: "Explorer 124270 steel" }),
      row({ sourceLine: 90, identity: "Rolex Explorer 36 124270 M1 Geometry" }),
    ]);
    expect(result.accepted).toHaveLength(1);
    expect(result.collapsed).toHaveLength(1);
  });

  it("rejects a conflicting duplicate and names the fields", () => {
    const result = dedupeSheetRows([
      row({ sourceLine: 2, priceUsdMinor: 1_440_000 }),
      row({ sourceLine: 55, priceUsdMinor: 1_590_000 }),
    ]);
    expect(result.accepted).toHaveLength(0);
    expect(result.conflicts).toEqual([
      {
        referenceCode: "126610LN",
        sourceLines: [2, 55],
        fields: ["priceUsdMinor"],
      },
    ]);
  });

  it("reports every differing field in a conflict", () => {
    const result = dedupeSheetRows([
      row({ sourceLine: 2 }),
      row({ sourceLine: 55, priceUsdMinor: 1, caseDiameterMm: 40 }),
    ]);
    expect(result.conflicts[0]?.fields).toEqual([
      "caseDiameterMm",
      "priceUsdMinor",
    ]);
  });

  it("keeps distinct references separate", () => {
    const result = dedupeSheetRows([
      row({ sourceLine: 2 }),
      row({ sourceLine: 3, referenceCode: "126610LV" }),
    ]);
    expect(result.accepted).toHaveLength(2);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/sheet-dedup.test.ts`
Expected: FAIL — `Failed to resolve import "./sheet-dedup"`.

- [ ] **Step 3: Write the implementation**

Create `app/domain/sheet-dedup.ts`:

```typescript
import type { SheetRow } from "./sheet-intake";

const ARTIFACT_MARKERS = [
  "needs research",
  "нет точного варианта в seed-catalogue",
];

export function isWorkbookArtifact(cells: string[]) {
  return cells.some((cell) => {
    const text = cell.normalize("NFKC").trim().toLocaleLowerCase("ru-RU");
    return ARTIFACT_MARKERS.some((marker) => text.startsWith(marker));
  });
}

// `identity` and `sourceLine` are labels, not facts: two rows describing the
// same reference under different display names are the same watch.
const COMPARED_FIELDS = [
  "caliber",
  "caseDiameterMm",
  "caseShape",
  "caseThicknessMm",
  "complications",
  "crystal",
  "displayCaseback",
  "integratedBracelet",
  "lugToLugMm",
  "lugWidthMm",
  "microAdjustment",
  "movementConstruction",
  "movementType",
  "nickelContactRisk",
  "positioningGroup",
  "positioningLine",
  "priceUsdMinor",
  "productUrl",
  "waterResistanceM",
  "wearingScenarios",
] as const satisfies readonly (keyof SheetRow)[];

function differingFields(left: SheetRow, right: SheetRow) {
  return COMPARED_FIELDS.filter(
    (field) => JSON.stringify(left[field]) !== JSON.stringify(right[field]),
  );
}

export function dedupeSheetRows(rows: SheetRow[]) {
  const grouped = new Map<string, SheetRow[]>();
  for (const row of rows) {
    const existing = grouped.get(row.referenceCode);
    if (existing) existing.push(row);
    else grouped.set(row.referenceCode, [row]);
  }

  const accepted: SheetRow[] = [];
  const collapsed: { referenceCode: string; sourceLines: number[] }[] = [];
  const conflicts: {
    referenceCode: string;
    sourceLines: number[];
    fields: string[];
  }[] = [];

  for (const [referenceCode, group] of grouped) {
    const first = group[0]!;
    if (group.length === 1) {
      accepted.push(first);
      continue;
    }
    const fields = [
      ...new Set(
        group.slice(1).flatMap((candidate) => differingFields(first, candidate)),
      ),
    ].sort();
    const sourceLines = group.map((candidate) => candidate.sourceLine).sort(
      (a, b) => a - b,
    );
    if (fields.length === 0) {
      accepted.push(first);
      collapsed.push({ referenceCode, sourceLines });
      continue;
    }
    conflicts.push({ referenceCode, sourceLines, fields });
  }

  return { accepted, collapsed, conflicts };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/domain/sheet-dedup.test.ts`
Expected: PASS, 9 tests.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/domain/sheet-dedup.ts app/domain/sheet-dedup.test.ts
git commit -m "$(cat <<'MSG'
add sheet deduplication with conflict rejection

Collapses exact duplicate references, drops workbook artifact rows, and
rejects same-reference conflicts for owner resolution rather than
choosing a winner automatically.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 5: Additive catalogue schema fields

**Files:**

- Modify: `app/domain/catalogue.ts`
- Modify: `app/domain/catalogue.test.ts`

**Interfaces:**

- Consumes: `CASE_SHAPES`, `microAdjustmentSchema` from Task 3.
- Produces: `seedReferenceVariantSchema` gains optional `geometry.caseShape`, `materials.displayCaseback`, `movement.construction`, `operation.microAdjustment`, `positioningLine`, `positioningGroup`, `wearingScenarios`, `complicationSlugs`. New `EvidenceField` members: `caseShape`, `displayCaseback`, `movementConstruction`, `microAdjustment`, `positioning`, `wearingScenarios`, `complicationSlugs`.

All new fields default to `null` or `[]` so the existing 71 variants and every v2 code path keep parsing. `dateStatus` and `eligibleEnvironments` stay until Task 15.

- [ ] **Step 1: Write the failing test**

Append to `app/domain/catalogue.test.ts`:

```typescript
describe("sheet-native catalogue fields", () => {
  it("accepts a variant carrying the new sheet fields", () => {
    const variant = structuredClone(baseVariant);
    variant.geometry.caseShape = "round";
    variant.materials.displayCaseback = true;
    variant.movement.construction = "manufacture";
    variant.operation.microAdjustment = {
      present: true,
      systemName: "Glidelock",
      rangeMm: 20,
    };
    variant.positioningLine = "мировой эталон дайвера";
    variant.positioningGroup = "instrument";
    variant.wearingScenarios = ["sport", "diving"];
    variant.complicationSlugs = ["date", "dive_bezel"];
    expect(() => seedReferenceVariantSchema.parse(variant)).not.toThrow();
  });

  it("defaults the new fields when they are absent", () => {
    const parsed = seedReferenceVariantSchema.parse(
      structuredClone(baseVariant),
    );
    expect(parsed.geometry.caseShape).toBeNull();
    expect(parsed.materials.displayCaseback).toBeNull();
    expect(parsed.movement.construction).toBeNull();
    expect(parsed.operation.microAdjustment).toBeNull();
    expect(parsed.positioningLine).toBeNull();
    expect(parsed.positioningGroup).toBeNull();
    expect(parsed.wearingScenarios).toEqual([]);
    expect(parsed.complicationSlugs).toEqual([]);
  });

  it("rejects an unknown case shape", () => {
    const variant = structuredClone(baseVariant);
    variant.geometry.caseShape = "triangular";
    expect(() => seedReferenceVariantSchema.parse(variant)).toThrow();
  });

  it("rejects a micro-adjustment range without a positive value", () => {
    const variant = structuredClone(baseVariant);
    variant.operation.microAdjustment = {
      present: true,
      systemName: "Glidelock",
      rangeMm: 0,
    };
    expect(() => seedReferenceVariantSchema.parse(variant)).toThrow();
  });
});
```

If `baseVariant` does not already exist in the test file, build it from the first entry of the parsed seed catalogue: `const baseVariant = structuredClone(seedCatalogue.variants[0]!)`.

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/catalogue.test.ts`
Expected: FAIL — `.strict()` rejects the unrecognised keys.

- [ ] **Step 3: Write the implementation**

In `app/domain/catalogue.ts`, import the shared types and extend the schema:

```typescript
import { CASE_SHAPES, microAdjustmentSchema } from "./sheet-intake";
```

Inside `geometry`, add:

```typescript
        caseShape: z.enum(CASE_SHAPES).nullable().default(null),
```

Inside `materials`, add:

```typescript
        displayCaseback: z.boolean().nullable().default(null),
```

Inside `movement`, add:

```typescript
        construction: z
          .enum(["mass_produced", "manufacture"])
          .nullable()
          .default(null),
```

Inside `operation`, add:

```typescript
        microAdjustment: microAdjustmentSchema.nullable().default(null),
```

At the top level of `seedReferenceVariantSchema`, add:

```typescript
    positioningLine: z.string().trim().min(1).max(300).nullable().default(null),
    positioningGroup: z.string().min(1).nullable().default(null),
    wearingScenarios: z.array(z.string().min(1)).default([]),
    complicationSlugs: z.array(z.string().min(1)).default([]),
```

Extend the `EvidenceField` union with:

```typescript
  | "caseShape"
  | "displayCaseback"
  | "movementConstruction"
  | "microAdjustment"
  | "positioning"
  | "wearingScenarios"
  | "complicationSlugs"
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/domain/catalogue.test.ts`
Expected: PASS, including the four new tests.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/domain/catalogue.ts app/domain/catalogue.test.ts
git commit -m "$(cat <<'MSG'
add sheet-native fields to the catalogue schema

Adds case shape, display caseback, movement construction, structured
micro-adjustment, positioning, wearing scenarios, and complication slugs
as nullable additive fields with field-level evidence support.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 6: Vocabulary and catalogue-column migration

**Files:**

- Create: `db/migrations/0065_add_catalogue_vocabulary.sql`

**Interfaces:**

- Consumes: the slug set from Task 1.
- Produces: table `public.catalogue_vocabulary`; join tables `public.reference_variant_scenario` and `public.reference_variant_complication`; new nullable columns on the reference-variant table; RPC `public.catalogue_vocabulary_v1()`.

Confirm the reference-variant table's real name from `db/migrations/0001_reference_variant_catalogue.sql` before writing this migration and use it verbatim.

- [ ] **Step 1: Write the migration**

Create `db/migrations/0065_add_catalogue_vocabulary.sql`:

```sql
-- Sheet-native catalogue vocabulary and reference-variant columns.
-- Additive only: no table is dropped, truncated, or reset, and the v3
-- recommendation RPCs continue to work unchanged.

create table if not exists public.catalogue_vocabulary (
  id bigint generated always as identity primary key,
  kind text not null check (kind in
    ('wearing_scenario', 'complication', 'positioning_group')),
  slug text not null check (slug ~ '^[a-z0-9]+(_[a-z0-9]+)*$'),
  label_en text not null check (char_length(label_en) between 1 and 80),
  source_alias text[] not null default '{}',
  sort_order integer not null default 100 check (sort_order >= 0),
  active boolean not null default true,
  unique (kind, slug)
);

alter table public.catalogue_vocabulary enable row level security;

create index if not exists catalogue_vocabulary_kind_sort_idx
  on public.catalogue_vocabulary (kind, sort_order, slug);

alter table public.reference_variants
  add column if not exists case_shape text
    check (case_shape is null or case_shape in
      ('round', 'tonneau', 'rectangular', 'cushion', 'square', 'oval', 'other')),
  add column if not exists display_caseback boolean,
  add column if not exists movement_construction text
    check (movement_construction is null or movement_construction in
      ('mass_produced', 'manufacture')),
  add column if not exists micro_adjustment_present boolean,
  add column if not exists micro_adjustment_system text,
  add column if not exists micro_adjustment_range_mm numeric
    check (micro_adjustment_range_mm is null or micro_adjustment_range_mm > 0),
  add column if not exists positioning_line text,
  add column if not exists positioning_group text;

create table if not exists public.reference_variant_scenario (
  variant_id uuid not null
    references public.reference_variants (id) on delete cascade,
  vocabulary_id bigint not null
    references public.catalogue_vocabulary (id) on delete restrict,
  primary key (variant_id, vocabulary_id)
);

create table if not exists public.reference_variant_complication (
  variant_id uuid not null
    references public.reference_variants (id) on delete cascade,
  vocabulary_id bigint not null
    references public.catalogue_vocabulary (id) on delete restrict,
  primary key (variant_id, vocabulary_id)
);

alter table public.reference_variant_scenario enable row level security;
alter table public.reference_variant_complication enable row level security;

create index if not exists reference_variant_scenario_vocabulary_idx
  on public.reference_variant_scenario (vocabulary_id);
create index if not exists reference_variant_complication_vocabulary_idx
  on public.reference_variant_complication (vocabulary_id);

create or replace function public.catalogue_vocabulary_v1()
returns table (
  kind text,
  slug text,
  label_en text,
  source_alias text[],
  sort_order integer,
  active boolean
)
language sql
stable
security definer
set search_path = ''
as $$
  select kind, slug, label_en, source_alias, sort_order, active
  from public.catalogue_vocabulary
  where active
  order by kind, sort_order, slug;
$$;

revoke all on function public.catalogue_vocabulary_v1() from public;
grant execute on function public.catalogue_vocabulary_v1() to anon, authenticated;
```

- [ ] **Step 2: Append the vocabulary seed rows**

Generate the `insert` from `BUNDLED_VOCABULARY` so the database and the bundled fallback cannot drift. Append to the same migration:

```sql
insert into public.catalogue_vocabulary (kind, slug, label_en, source_alias, sort_order, active)
values
  ('wearing_scenario', 'everyday', 'Everyday', array['повседневно','везде','casual'], 10, true),
  ('wearing_scenario', 'office', 'Office', array['офис'], 20, true)
  -- ... one row per entry in BUNDLED_VOCABULARY, in the same order ...
on conflict (kind, slug) do update set
  label_en = excluded.label_en,
  source_alias = excluded.source_alias,
  sort_order = excluded.sort_order,
  active = excluded.active;
```

Write every bundled row. Do not abbreviate the list in the committed file.

- [ ] **Step 3: Verify the reference-variant table name**

Run: `grep -n "create table" db/migrations/0001_reference_variant_catalogue.sql`
Expected: confirms the table name used above. Correct the migration if it differs.

- [ ] **Step 4: Apply the migration**

Apply through the Supabase MCP `apply_migration` tool with name `add_catalogue_vocabulary`. Then verify:

```sql
select kind, count(*) from public.catalogue_vocabulary group by kind order by kind;
```

Expected: `complication` 20, `positioning_group` 11, `wearing_scenario` ~44.

- [ ] **Step 5: Run advisors and commit**

Run the Supabase `get_advisors` tool for both `security` and `performance`. Expected: no new critical finding for these objects.

```bash
npm run check
git add db/migrations/0065_add_catalogue_vocabulary.sql
git commit -m "$(cat <<'MSG'
add catalogue vocabulary tables and sheet columns

Adds the vocabulary table with RLS, scenario and complication join
tables, additive reference-variant columns, and a read-only vocabulary
RPC. The v3 recommendation RPCs are untouched.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 7: Vocabulary server store

**Files:**

- Create: `app/domain/catalogue-vocabulary.server.ts`
- Test: `app/domain/catalogue-vocabulary.server.test.ts`

**Interfaces:**

- Consumes: `BUNDLED_VOCABULARY`, `vocabularyRowSchema` from Task 1; RPC `catalogue_vocabulary_v1` from Task 6.
- Produces: `CATALOGUE_VOCABULARY_RPC`, `loadCatalogueVocabulary(options?): Promise<readonly VocabularyRow[]>`.

Mirrors `discovery-store.server.ts`: the bundled list is an all-or-nothing fallback whenever the database is unconfigured, slow, failing, or returns a shape that does not validate.

- [ ] **Step 1: Write the failing test**

Create `app/domain/catalogue-vocabulary.server.test.ts`:

```typescript
import { describe, expect, it, vi } from "vitest";

import { BUNDLED_VOCABULARY } from "./catalogue-vocabulary";
import { loadCatalogueVocabulary } from "./catalogue-vocabulary.server";

const env = {
  SUPABASE_URL: "https://example.supabase.co",
  SUPABASE_PUBLISHABLE_KEY: "publishable-key",
};

function jsonResponse(body: unknown, status = 200) {
  return new Response(JSON.stringify(body), {
    status,
    headers: { "content-type": "application/json" },
  });
}

describe("loadCatalogueVocabulary", () => {
  it("falls back to the bundled vocabulary when unconfigured", async () => {
    const fetchImpl = vi.fn();
    await expect(loadCatalogueVocabulary({ env: {}, fetchImpl })).resolves.toBe(
      BUNDLED_VOCABULARY,
    );
    expect(fetchImpl).not.toHaveBeenCalled();
  });

  it("returns validated database rows", async () => {
    const fetchImpl = vi.fn().mockResolvedValue(
      jsonResponse([
        {
          kind: "positioning_group",
          slug: "instrument",
          label_en: "Instrument",
          source_alias: [],
          sort_order: 10,
          active: true,
        },
      ]),
    );
    await expect(
      loadCatalogueVocabulary({ env, fetchImpl }),
    ).resolves.toEqual([
      {
        kind: "positioning_group",
        slug: "instrument",
        labelEn: "Instrument",
        sourceAliases: [],
        sortOrder: 10,
        active: true,
      },
    ]);
  });

  it("falls back when the RPC fails", async () => {
    const fetchImpl = vi.fn().mockResolvedValue(jsonResponse({}, 500));
    await expect(loadCatalogueVocabulary({ env, fetchImpl })).resolves.toBe(
      BUNDLED_VOCABULARY,
    );
  });

  it("falls back when the response shape is invalid", async () => {
    const fetchImpl = vi
      .fn()
      .mockResolvedValue(jsonResponse([{ kind: "nope", slug: "x" }]));
    await expect(loadCatalogueVocabulary({ env, fetchImpl })).resolves.toBe(
      BUNDLED_VOCABULARY,
    );
  });

  it("falls back when the request throws", async () => {
    const fetchImpl = vi.fn().mockRejectedValue(new Error("timeout"));
    await expect(loadCatalogueVocabulary({ env, fetchImpl })).resolves.toBe(
      BUNDLED_VOCABULARY,
    );
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/catalogue-vocabulary.server.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Write the implementation**

Create `app/domain/catalogue-vocabulary.server.ts`:

```typescript
import { z } from "zod";

import {
  BUNDLED_VOCABULARY,
  vocabularyRowSchema,
  type VocabularyRow,
} from "./catalogue-vocabulary";

export const CATALOGUE_VOCABULARY_RPC = "catalogue_vocabulary_v1" as const;
const STORE_TIMEOUT_MS = 2_000;

const databaseRowsSchema = z.array(
  z
    .object({
      kind: z.string(),
      slug: z.string(),
      label_en: z.string(),
      source_alias: z.array(z.string()),
      sort_order: z.number(),
      active: z.boolean(),
    })
    .strict()
    .transform((row) =>
      vocabularyRowSchema.parse({
        kind: row.kind,
        slug: row.slug,
        labelEn: row.label_en,
        sourceAliases: row.source_alias,
        sortOrder: row.sort_order,
        active: row.active,
      }),
    ),
);

type Options = {
  env?: Partial<
    Pick<NodeJS.ProcessEnv, "SUPABASE_URL" | "SUPABASE_PUBLISHABLE_KEY">
  >;
  fetchImpl?: typeof fetch;
  timeoutMs?: number;
};

export async function loadCatalogueVocabulary({
  env = process.env,
  fetchImpl = fetch,
  timeoutMs = STORE_TIMEOUT_MS,
}: Options = {}): Promise<readonly VocabularyRow[]> {
  const supabaseUrl = env.SUPABASE_URL?.trim();
  const publishableKey = env.SUPABASE_PUBLISHABLE_KEY?.trim();
  if (!supabaseUrl || !publishableKey) return BUNDLED_VOCABULARY;

  try {
    const response = await fetchImpl(
      new URL(`/rest/v1/rpc/${CATALOGUE_VOCABULARY_RPC}`, supabaseUrl),
      {
        method: "POST",
        headers: { apikey: publishableKey, "content-type": "application/json" },
        body: "{}",
        signal: AbortSignal.timeout(timeoutMs),
      },
    );
    if (!response.ok) return BUNDLED_VOCABULARY;
    const parsed = databaseRowsSchema.safeParse(await response.json());
    return parsed.success && parsed.data.length > 0
      ? parsed.data
      : BUNDLED_VOCABULARY;
  } catch {
    return BUNDLED_VOCABULARY;
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/domain/catalogue-vocabulary.server.test.ts`
Expected: PASS, 5 tests.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/domain/catalogue-vocabulary.server.ts app/domain/catalogue-vocabulary.server.test.ts
git commit -m "$(cat <<'MSG'
add catalogue vocabulary server store

Reads the vocabulary through a narrow read-only RPC and falls back to
the bundled list whenever the database is unconfigured, failing, slow,
or returns an invalid shape.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 8: Forward-migrate the 71 reviewed variants

**Files:**

- Create: `scripts/migrate-seed-catalogue-v3.ts`
- Modify: `data/catalogue/seed-catalogue.json`
- Modify: `package.json`

**Interfaces:**

- Consumes: the Task 5 schema, `BUNDLED_VOCABULARY` from Task 1.
- Produces: an npm script `migrate:seed-catalogue-v3`; a seed catalogue whose every variant carries `wearingScenarios` and `complicationSlugs`.

Spec section 8: `field_water_abuse` expands to `sport`, `diving`, `field`; `studio_desk_daily` to `office`, `everyday`, `smart_casual`; `formal_architectural` to `suit`, `evening`, `reception`. The v2 `complications` array plus `dateStatus` become `complicationSlugs`.

- [ ] **Step 1: Write the migration script**

Create `scripts/migrate-seed-catalogue-v3.ts`:

```typescript
import fs from "node:fs";
import path from "node:path";

const SCENARIOS: Record<string, string[]> = {
  field_water_abuse: ["sport", "diving", "field"],
  studio_desk_daily: ["office", "everyday", "smart_casual"],
  formal_architectural: ["suit", "evening", "reception"],
};

const COMPLICATIONS: Record<string, string> = {
  gmt: "gmt",
  chronograph: "chronograph",
  moonphase: "moonphase",
  power_reserve: "power_reserve",
  alarm: "alarm",
  world_time: "world_time",
  perpetual_calendar: "perpetual_calendar",
};

type Variant = {
  eligibleEnvironments: string[];
  complications: string[];
  dateStatus: "present" | "absent";
  wearingScenarios?: string[];
  complicationSlugs?: string[];
};

const file = path.join(process.cwd(), "data/catalogue/seed-catalogue.json");
const catalogue = JSON.parse(fs.readFileSync(file, "utf8")) as {
  variants: Variant[];
};

for (const variant of catalogue.variants) {
  variant.wearingScenarios = [
    ...new Set(
      variant.eligibleEnvironments.flatMap(
        (environment) => SCENARIOS[environment] ?? [],
      ),
    ),
  ].sort();

  const slugs = variant.complications
    .map((complication) => COMPLICATIONS[complication])
    .filter((slug): slug is string => Boolean(slug));
  if (variant.dateStatus === "present") slugs.push("date");
  if (slugs.length === 0) slugs.push("time_only");
  variant.complicationSlugs = [...new Set(slugs)].sort();
}

fs.writeFileSync(file, `${JSON.stringify(catalogue, null, 2)}\n`);
console.log(`Migrated ${catalogue.variants.length} variants to v3 fields.`);
```

`power_reserve`, `alarm`, `world_time`, and `perpetual_calendar` are not in the sheet sample but exist in the v2 enum. Add matching `complication` rows to `BUNDLED_VOCABULARY` and to migration `0065` before running this script, so no existing variant loses a complication.

- [ ] **Step 2: Add the four missing complication slugs**

In `app/domain/catalogue-vocabulary.ts`, add to the complication block:

```typescript
  complication("power_reserve", "Power reserve", ["запас хода"], 210),
  complication("alarm", "Alarm", ["будильник"], 220),
  complication("world_time", "World time", ["мировое время"], 230),
  complication("perpetual_calendar", "Perpetual calendar", [
    "вечный календарь",
  ], 240),
```

Add the same four rows to `db/migrations/0065_add_catalogue_vocabulary.sql` and re-apply it — the seed insert is idempotent via `on conflict do update`.

- [ ] **Step 3: Register and run the script**

Add to `package.json` scripts:

```json
    "migrate:seed-catalogue-v3": "tsx scripts/migrate-seed-catalogue-v3.ts",
```

Run: `npm run migrate:seed-catalogue-v3`
Expected: `Migrated 71 variants to v3 fields.`

- [ ] **Step 4: Verify every variant gained both fields**

Run:

```bash
node -e "const c=require('./data/catalogue/seed-catalogue.json');const bad=c.variants.filter(v=>!v.wearingScenarios?.length||!v.complicationSlugs?.length);console.log('variants',c.variants.length,'incomplete',bad.length)"
```

Expected: `variants 71 incomplete 0`.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add scripts/migrate-seed-catalogue-v3.ts data/catalogue/seed-catalogue.json package.json app/domain/catalogue-vocabulary.ts db/migrations/0065_add_catalogue_vocabulary.sql
git commit -m "$(cat <<'MSG'
forward-migrate reviewed variants to v3 catalogue fields

Expands the three deployment environments into scenario slugs and folds
date status into complication slugs so no reviewed variant loses a fact.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 9: Sheet import script

**Files:**

- Create: `scripts/import-model-sheet.ts`
- Modify: `package.json`

**Interfaces:**

- Consumes: `parseSheetRow`, `isWorkbookArtifact`, `dedupeSheetRows` from Tasks 3-4.
- Produces: npm script `import:model-sheet`; writes `data/research/model-sheet-intake.json` and prints a rejection report.

The script never writes a rejected row and never resolves a conflict on the owner's behalf.

- [ ] **Step 1: Write the script**

Create `scripts/import-model-sheet.ts`:

```typescript
import fs from "node:fs";
import path from "node:path";

import { dedupeSheetRows, isWorkbookArtifact } from "../app/domain/sheet-dedup";
import { parseSheetRow, type SheetRow } from "../app/domain/sheet-intake";

const input = process.argv[2];
if (!input) {
  console.error("Usage: npm run import:model-sheet -- <path-to-tsv>");
  process.exit(1);
}

const source = fs.readFileSync(path.resolve(input), "utf8");
const lines = source.split(/\r?\n/);
const parsed: SheetRow[] = [];
const rejected: { sourceLine: number; reasons: string[] }[] = [];
let artifacts = 0;

for (const [index, line] of lines.entries()) {
  const sourceLine = index + 1;
  if (line.trim().length === 0) continue;
  const cells = line.split("\t");
  if (cells[0]?.trim() === "Модель / reference") continue;
  if (isWorkbookArtifact(cells)) {
    artifacts += 1;
    continue;
  }
  const result = parseSheetRow(cells, sourceLine);
  if (result.ok) parsed.push(result.row);
  else rejected.push({ sourceLine, reasons: result.reasons });
}

const { accepted, collapsed, conflicts } = dedupeSheetRows(parsed);

console.log(`Parsed ${parsed.length} rows; dropped ${artifacts} artifacts.`);
console.log(`Accepted ${accepted.length} unique references.`);
console.log(`Collapsed ${collapsed.length} exact duplicate groups.`);
for (const group of collapsed) {
  console.log(`  ${group.referenceCode}: lines ${group.sourceLines.join(", ")}`);
}
console.log(`Rejected ${conflicts.length} conflicting duplicate groups.`);
for (const conflict of conflicts) {
  console.log(
    `  ${conflict.referenceCode}: lines ${conflict.sourceLines.join(", ")} differ in ${conflict.fields.join(", ")}`,
  );
}
console.log(`Rejected ${rejected.length} unparseable rows.`);
for (const rejection of rejected) {
  for (const reason of rejection.reasons) console.log(`  ${reason}`);
}

const output = path.join(process.cwd(), "data/research/model-sheet-intake.json");
fs.writeFileSync(
  output,
  `${JSON.stringify({ importedAt: new Date().toISOString(), accepted }, null, 2)}\n`,
);
console.log(`Wrote ${accepted.length} rows to ${output}.`);

if (conflicts.length > 0 || rejected.length > 0) process.exitCode = 1;
```

- [ ] **Step 2: Register the script**

Add to `package.json` scripts:

```json
    "import:model-sheet": "tsx scripts/import-model-sheet.ts",
```

- [ ] **Step 3: Run it against the owner's sample**

Save the owner's pasted rows to `data/research/model-sheet-sample.tsv`, then run:

`npm run import:model-sheet -- data/research/model-sheet-sample.tsv`

Expected: the report names the Sky-Dweller, Submariner, Yacht-Master, and Explorer duplicate groups from spec section 8, drops seven artifacts, and exits non-zero while conflicts remain.

- [ ] **Step 4: Report which reviewed variants the sheet would supersede**

Spec section 8 requires that a sheet row supplying an already-reviewed reference
supersedes it. Promotion into the accepted catalogue still goes through the
repository's evidence and review gates, so the importer reports the overlap and
never merges silently. Append to `scripts/import-model-sheet.ts`, before the
output is written:

```typescript
import { seedCatalogue } from "../app/domain/seed-catalogue";

const reviewed = new Map(
  seedCatalogue.variants.map((variant) => [
    variant.referenceCode.toLocaleUpperCase("en-US"),
    variant.id,
  ]),
);
const superseding = accepted.filter((row) =>
  reviewed.has(row.referenceCode.toLocaleUpperCase("en-US")),
);
console.log(
  `${superseding.length} sheet rows supersede an already-reviewed variant:`,
);
for (const row of superseding) {
  console.log(
    `  ${row.referenceCode} supersedes ${reviewed.get(row.referenceCode.toLocaleUpperCase("en-US"))}`,
  );
}
```

Run the importer again and record the superseding list. These rows are not
promoted by this task; they are the owner's review queue.

- [ ] **Step 5: Record the outcome**

Write the exact counts, the conflict list, and the superseding list into the
commit message so the owner can resolve them.

- [ ] **Step 6: Run the full gate and commit**

```bash
npm run check
git add scripts/import-model-sheet.ts package.json
git commit -m "$(cat <<'MSG'
add model sheet importer with rejection reporting

Parses the tab-separated sheet, drops workbook artifacts, collapses
exact duplicates, and exits non-zero listing conflicting duplicates and
unparseable rows for owner resolution.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 10: Questionnaire v3 contract

**Files:**

- Create: `app/domain/questionnaire-v3.ts`
- Test: `app/domain/questionnaire-v3.test.ts`

**Interfaces:**

- Consumes: `CASE_SHAPES` from Task 3.
- Produces:
  - `QUESTIONNAIRE_V3_VERSION = 3`, `QUESTIONNAIRE_V3_STORAGE_KEY = "the-reserve:diagnostic:v3"`
  - `WATER_RESISTANCE_MINIMUMS`, `MOVEMENT_TYPE_CHOICES`, `MOVEMENT_CONSTRUCTIONS`, `CRYSTAL_CHOICES`, `ALLERGY_CONSTRAINTS_V3`
  - `profileV3Schema`, `type ProfileV3`
  - `normalizeProfileV3(profile: ProfileV3)` returning `{ ...profile, derived: { priceBand, effectiveBudgetCeiling } }`

`CURRENCIES`, `PRICE_BANDS`, and `derivePriceBand` are reused from `questionnaire.ts` — do not duplicate them.

- [ ] **Step 1: Write the failing test**

Create `app/domain/questionnaire-v3.test.ts`:

```typescript
import { describe, expect, it } from "vitest";

import { normalizeProfileV3, profileV3Schema } from "./questionnaire-v3";

const valid = {
  version: 3 as const,
  budgetCurrency: "USD" as const,
  budgetMax: 15_000,
  wearingScenarios: ["office", "sport"],
  minimumWaterResistanceM: 100,
  caseDiameterMinMm: 36,
  caseDiameterMaxMm: 41,
  movementTypes: ["automatic" as const],
  requiredComplications: ["date"],
  allergyConstraint: "none" as const,
};

describe("profileV3Schema", () => {
  it("accepts a minimal valid profile", () => {
    expect(() => profileV3Schema.parse(valid)).not.toThrow();
  });

  it("accepts optional soft preferences", () => {
    expect(() =>
      profileV3Schema.parse({
        ...valid,
        maxCaseThicknessMm: 12,
        caseShape: "round",
        movementConstruction: "manufacture",
        displayCaseback: true,
        crystal: "sapphire",
        microAdjustmentRequired: true,
      }),
    ).not.toThrow();
  });

  it("rejects an inverted diameter range", () => {
    expect(() =>
      profileV3Schema.parse({
        ...valid,
        caseDiameterMinMm: 42,
        caseDiameterMaxMm: 38,
      }),
    ).toThrow();
  });

  it("rejects an empty movement-type selection", () => {
    expect(() =>
      profileV3Schema.parse({ ...valid, movementTypes: [] }),
    ).toThrow();
  });

  it("rejects a duplicated movement type", () => {
    expect(() =>
      profileV3Schema.parse({
        ...valid,
        movementTypes: ["automatic", "automatic"],
      }),
    ).toThrow();
  });

  it("rejects an empty wearing-scenario selection", () => {
    expect(() =>
      profileV3Schema.parse({ ...valid, wearingScenarios: [] }),
    ).toThrow();
  });

  it("accepts an empty complication requirement", () => {
    expect(() =>
      profileV3Schema.parse({ ...valid, requiredComplications: [] }),
    ).not.toThrow();
  });

  it("rejects an unknown key", () => {
    expect(() =>
      profileV3Schema.parse({ ...valid, socialSignal: "quiet_continuity" }),
    ).toThrow();
  });

  it("rejects a non-positive budget", () => {
    expect(() => profileV3Schema.parse({ ...valid, budgetMax: 0 })).toThrow();
  });
});

describe("normalizeProfileV3", () => {
  it("derives the price band and ceiling", () => {
    const normalized = normalizeProfileV3(profileV3Schema.parse(valid));
    expect(normalized.derived.priceBand).toBe("10000_15000");
    expect(normalized.derived.effectiveBudgetCeiling).toBe(15_000);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/questionnaire-v3.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Write the implementation**

Create `app/domain/questionnaire-v3.ts`:

```typescript
import { z } from "zod";

import { CASE_SHAPES } from "./sheet-intake";
import { CURRENCIES, derivePriceBand } from "./questionnaire";

export const QUESTIONNAIRE_V3_VERSION = 3 as const;
export const QUESTIONNAIRE_V3_STORAGE_KEY =
  "the-reserve:diagnostic:v3" as const;

export const WATER_RESISTANCE_MINIMUMS = [0, 30, 50, 100, 200, 300] as const;
export const MOVEMENT_TYPE_CHOICES = [
  "automatic",
  "manual",
  "quartz",
  "solar",
  "spring_drive",
  "hybrid",
] as const;
export const MOVEMENT_CONSTRUCTIONS = ["mass_produced", "manufacture"] as const;
export const CRYSTAL_CHOICES = [
  "sapphire",
  "mineral",
  "acrylic",
  "other",
] as const;
export const ALLERGY_CONSTRAINTS_V3 = ["none", "nickel_contact"] as const;

export const profileV3Schema = z
  .object({
    version: z.literal(QUESTIONNAIRE_V3_VERSION),
    budgetCurrency: z.enum(CURRENCIES),
    budgetMax: z.number().finite().positive().max(10_000_000),
    wearingScenarios: z.array(z.string().min(1)).min(1).max(12),
    minimumWaterResistanceM: z.number().int().nonnegative().max(12_000),
    caseDiameterMinMm: z.number().finite().min(20).max(60),
    caseDiameterMaxMm: z.number().finite().min(20).max(60),
    movementTypes: z.array(z.enum(MOVEMENT_TYPE_CHOICES)).min(1),
    requiredComplications: z.array(z.string().min(1)).max(24),
    allergyConstraint: z.enum(ALLERGY_CONSTRAINTS_V3),
    maxCaseThicknessMm: z.number().finite().min(3).max(30).optional(),
    caseShape: z.enum(CASE_SHAPES).optional(),
    movementConstruction: z.enum(MOVEMENT_CONSTRUCTIONS).optional(),
    displayCaseback: z.boolean().optional(),
    crystal: z.enum(CRYSTAL_CHOICES).optional(),
    microAdjustmentRequired: z.boolean().optional(),
  })
  .strict()
  .superRefine((profile, context) => {
    const unique = (values: string[]) => new Set(values).size === values.length;
    if (profile.caseDiameterMaxMm < profile.caseDiameterMinMm) {
      context.addIssue({
        code: "custom",
        path: ["caseDiameterMaxMm"],
        message: "The maximum diameter cannot be below the minimum.",
      });
    }
    if (!unique(profile.movementTypes)) {
      context.addIssue({
        code: "custom",
        path: ["movementTypes"],
        message: "A movement type can only be selected once.",
      });
    }
    if (!unique(profile.wearingScenarios)) {
      context.addIssue({
        code: "custom",
        path: ["wearingScenarios"],
        message: "A wearing scenario can only be selected once.",
      });
    }
    if (!unique(profile.requiredComplications)) {
      context.addIssue({
        code: "custom",
        path: ["requiredComplications"],
        message: "A complication can only be selected once.",
      });
    }
  });

export type ProfileV3 = z.infer<typeof profileV3Schema>;

export function normalizeProfileV3(profile: ProfileV3) {
  return {
    ...profile,
    derived: {
      priceBand: derivePriceBand(profile.budgetMax),
      effectiveBudgetCeiling: profile.budgetMax,
    },
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/domain/questionnaire-v3.test.ts`
Expected: PASS, 10 tests.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/domain/questionnaire-v3.ts app/domain/questionnaire-v3.test.ts
git commit -m "$(cat <<'MSG'
add questionnaire v3 contract

Defines the thirteen-question profile: seven hard dimensions, six
optional soft preferences, and a derived price band reusing the shared
currency and band constants.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 11: Engine v3 hard filters

**Files:**

- Modify: `app/domain/recommendation.ts`
- Modify: `app/domain/recommendation.test.ts`

**Interfaces:**

- Consumes: `ProfileV3` from Task 10; `SeedCatalogue`, `hasVerifiedField` from `catalogue.ts`.
- Produces:
  - `RECOMMENDATION_V3_ENGINE_VERSION = 3`
  - `HARD_REASON_CODES_V3` — the seven codes from spec section 6
  - `MISSING_FACT_CODES_V3` — the six codes
  - `evaluateHardFiltersV3(variant, profile, asOfMs): { hardReasons: HardReasonCodeV3[]; missingFacts: MissingFactCodeV3[] }`

v2 exports stay untouched until Task 15.

- [ ] **Step 1: Write the failing test**

Append to `app/domain/recommendation.test.ts`:

```typescript
import { evaluateHardFiltersV3 } from "./recommendation";
import { profileV3Schema } from "./questionnaire-v3";
import { seedCatalogue } from "./seed-catalogue";

const asOfMs = Date.parse("2026-09-03T00:00:00.000Z");

function v3Profile(overrides: Record<string, unknown> = {}) {
  return profileV3Schema.parse({
    version: 3,
    budgetCurrency: "USD",
    budgetMax: 1_000_000,
    wearingScenarios: ["office", "everyday", "sport", "diving", "field",
      "smart_casual", "suit", "evening", "reception"],
    minimumWaterResistanceM: 0,
    caseDiameterMinMm: 20,
    caseDiameterMaxMm: 60,
    movementTypes: ["automatic", "manual", "quartz", "solar", "spring_drive",
      "hybrid"],
    requiredComplications: [],
    allergyConstraint: "none",
    ...overrides,
  });
}

describe("evaluateHardFiltersV3", () => {
  const variant = seedCatalogue.variants[0]!;

  it("passes a permissive profile", () => {
    const result = evaluateHardFiltersV3(variant, v3Profile(), asOfMs);
    expect(result.hardReasons).toEqual([]);
  });

  it("rejects a variant above the budget ceiling", () => {
    const result = evaluateHardFiltersV3(
      variant,
      v3Profile({ budgetMax: 1 }),
      asOfMs,
    );
    expect(result.hardReasons).toContain("over_budget");
  });

  it("rejects a diameter outside the requested range", () => {
    const result = evaluateHardFiltersV3(
      variant,
      v3Profile({ caseDiameterMinMm: 59, caseDiameterMaxMm: 60 }),
      asOfMs,
    );
    expect(result.hardReasons).toContain("case_diameter_out_of_range");
  });

  it("rejects insufficient water resistance", () => {
    const result = evaluateHardFiltersV3(
      variant,
      v3Profile({ minimumWaterResistanceM: 12_000 }),
      asOfMs,
    );
    expect(result.hardReasons).toContain("water_resistance_below_minimum");
  });

  it("rejects a movement type outside the accepted set", () => {
    const other = variant.movement.type === "quartz" ? "manual" : "quartz";
    const result = evaluateHardFiltersV3(
      variant,
      v3Profile({ movementTypes: [other] }),
      asOfMs,
    );
    expect(result.hardReasons).toContain("movement_type_mismatch");
  });

  it("rejects a variant with no overlapping wearing scenario", () => {
    const result = evaluateHardFiltersV3(
      variant,
      v3Profile({ wearingScenarios: ["caving"] }),
      asOfMs,
    );
    expect(result.hardReasons).toContain("scenario_mismatch");
  });

  it("rejects a missing required complication", () => {
    const result = evaluateHardFiltersV3(
      variant,
      v3Profile({ requiredComplications: ["regatta_timer"] }),
      asOfMs,
    );
    expect(result.hardReasons).toContain("missing_complication");
  });

  it("reports an unverified diameter as a missing fact, not a pass", () => {
    const unverified = structuredClone(variant);
    unverified.geometry.caseDiameterMm = null;
    const result = evaluateHardFiltersV3(
      unverified,
      v3Profile({ caseDiameterMinMm: 36, caseDiameterMaxMm: 40 }),
      asOfMs,
    );
    expect(result.missingFacts).toContain("case_diameter");
    expect(result.hardReasons).not.toContain("case_diameter_out_of_range");
  });

  it("reports an unverified nickel risk when an allergy is declared", () => {
    const unverified = structuredClone(variant);
    unverified.operation.nickelContactRisk = null;
    const result = evaluateHardFiltersV3(
      unverified,
      v3Profile({ allergyConstraint: "nickel_contact" }),
      asOfMs,
    );
    expect(result.missingFacts).toContain("nickel_contact_risk");
  });

  it("rejects a confirmed nickel risk under an allergy constraint", () => {
    const risky = structuredClone(variant);
    risky.operation.nickelContactRisk = "confirmed";
    risky.evidence.push({
      sourceId: variant.evidence[0]!.sourceId,
      fields: ["nickelContactRisk"],
    });
    const result = evaluateHardFiltersV3(
      risky,
      v3Profile({ allergyConstraint: "nickel_contact" }),
      asOfMs,
    );
    expect(result.hardReasons).toContain("allergy_risk");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/recommendation.test.ts`
Expected: FAIL — `evaluateHardFiltersV3` is not exported.

- [ ] **Step 3: Write the implementation**

Append to `app/domain/recommendation.ts`:

```typescript
import type { ProfileV3 } from "./questionnaire-v3";

export const RECOMMENDATION_V3_ENGINE_VERSION = 3 as const;

export const HARD_REASON_CODES_V3 = [
  "over_budget",
  "case_diameter_out_of_range",
  "water_resistance_below_minimum",
  "movement_type_mismatch",
  "scenario_mismatch",
  "missing_complication",
  "allergy_risk",
] as const;

export const MISSING_FACT_CODES_V3 = [
  "fx_rate",
  "price",
  "case_diameter",
  "water_resistance",
  "wearing_scenarios",
  "nickel_contact_risk",
] as const;

export type HardReasonCodeV3 = (typeof HARD_REASON_CODES_V3)[number];
export type MissingFactCodeV3 = (typeof MISSING_FACT_CODES_V3)[number];

export function evaluateHardFiltersV3(
  variant: SeedReferenceVariant,
  profile: ProfileV3,
  asOfMs: number,
  fx?: SeedCatalogue["fx"],
) {
  const hardReasons: HardReasonCodeV3[] = [];
  const missingFacts: MissingFactCodeV3[] = [];
  const reject = (code: HardReasonCodeV3) => {
    if (!hardReasons.includes(code)) hardReasons.push(code);
  };
  const missing = (code: MissingFactCodeV3) => {
    if (!missingFacts.includes(code)) missingFacts.push(code);
  };

  const requiresFx = fx !== undefined && variant.price.currency !== profile.budgetCurrency;
  const fxUsable =
    !requiresFx || asOfMs <= Date.parse(fx.staleAfter);
  if (requiresFx && !fxUsable) missing("fx_rate");

  if (
    !hasVerifiedField(variant, "price") ||
    asOfMs > Date.parse(variant.price.staleAfter)
  ) {
    missing("price");
  } else if (fxUsable) {
    const amountMinor = fx
      ? convertMinorCurrency(
          variant.price.amountMinor,
          variant.price.currency,
          profile.budgetCurrency,
          fx,
        )
      : variant.price.amountMinor;
    if (amountMinor > Math.round(profile.budgetMax * 100)) reject("over_budget");
  }

  const diameter = variant.geometry.caseDiameterMm;
  if (diameter === null || !hasVerifiedField(variant, "caseDiameterMm")) {
    missing("case_diameter");
  } else if (
    diameter < profile.caseDiameterMinMm ||
    diameter > profile.caseDiameterMaxMm
  ) {
    reject("case_diameter_out_of_range");
  }

  if (profile.minimumWaterResistanceM > 0) {
    const resistance = variant.operation.waterResistanceM;
    if (resistance === null || !hasVerifiedField(variant, "waterResistanceM")) {
      missing("water_resistance");
    } else if (resistance < profile.minimumWaterResistanceM) {
      reject("water_resistance_below_minimum");
    }
  }

  if (!profile.movementTypes.includes(variant.movement.type)) {
    reject("movement_type_mismatch");
  }

  if (variant.wearingScenarios.length === 0) {
    missing("wearing_scenarios");
  } else if (
    !variant.wearingScenarios.some((scenario) =>
      profile.wearingScenarios.includes(scenario),
    )
  ) {
    reject("scenario_mismatch");
  }

  if (
    profile.requiredComplications.some(
      (complication) => !variant.complicationSlugs.includes(complication),
    )
  ) {
    reject("missing_complication");
  }

  if (profile.allergyConstraint === "nickel_contact") {
    const risk = variant.operation.nickelContactRisk;
    if (risk === null || !hasVerifiedField(variant, "nickelContactRisk")) {
      missing("nickel_contact_risk");
    } else if (risk !== "none_known") {
      reject("allergy_risk");
    }
  }

  return { hardReasons, missingFacts };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/domain/recommendation.test.ts`
Expected: PASS, including the 10 new tests.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/domain/recommendation.ts app/domain/recommendation.test.ts
git commit -m "$(cat <<'MSG'
add version-3 hard filter evaluation

Implements the seven v3 hard reasons and six missing-fact codes. An
unverified fact reports as missing rather than passing the filter.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 12: Engine v3 scoring and result assembly

**Files:**

- Modify: `app/domain/recommendation.ts`
- Modify: `app/domain/recommendation.test.ts`

**Interfaces:**

- Consumes: `evaluateHardFiltersV3` from Task 11.
- Produces:
  - `scoreVariantV3(variant, profile): { score: number; scoreTrace: ScoreFactor[] }`
  - `recommendWatchesV3(profile, catalogue, options?): RecommendationResultV3`
  - `type RecommendationResultV3` — same shape as `RecommendationResult` but with v3 code unions, plus `positioningLine` and `positioningGroup` on each candidate.

Soft factors per spec section 6: `diameter_centring`, `thickness_fit`, `shape_match`, `display_caseback_match`, `construction_match`, `crystal_match`, `micro_adjustment_match`, retaining `budget_headroom` and `evidence_completeness`. `shape_match`, `display_caseback_match`, `construction_match`, and `micro_adjustment_match` report through `unscoredPreferences` while the catalogue lacks their data.

- [ ] **Step 1: Write the failing test**

Append to `app/domain/recommendation.test.ts`:

```typescript
import { recommendWatchesV3, scoreVariantV3 } from "./recommendation";

describe("scoreVariantV3", () => {
  const variant = seedCatalogue.variants[0]!;

  it("rewards a diameter at the centre of the requested range", () => {
    const diameter = variant.geometry.caseDiameterMm!;
    const centred = scoreVariantV3(
      variant,
      v3Profile({
        caseDiameterMinMm: diameter - 2,
        caseDiameterMaxMm: diameter + 2,
      }),
    );
    const offset = scoreVariantV3(
      variant,
      v3Profile({
        caseDiameterMinMm: diameter,
        caseDiameterMaxMm: diameter + 10,
      }),
    );
    expect(centred.score).toBeGreaterThan(offset.score);
  });

  it("rewards a crystal match", () => {
    const matched = scoreVariantV3(
      variant,
      v3Profile({ crystal: variant.operation.crystal ?? undefined }),
    );
    expect(
      matched.scoreTrace.some((factor) => factor.factor === "crystal_match"),
    ).toBe(true);
  });

  it("contributes nothing when a soft preference is unset", () => {
    const trace = scoreVariantV3(variant, v3Profile()).scoreTrace;
    expect(trace.some((factor) => factor.factor === "crystal_match")).toBe(
      false,
    );
  });
});

describe("recommendWatchesV3", () => {
  it("returns at most three recommendations with distinct brands", () => {
    const result = recommendWatchesV3(v3Profile(), seedCatalogue, {
      asOf: "2026-09-03T00:00:00.000Z",
    });
    expect(result.recommendations.length).toBeLessThanOrEqual(3);
    const brands = result.recommendations.map((candidate) => candidate.brandSlug);
    expect(new Set(brands).size).toBe(brands.length);
  });

  it("is deterministic across repeated evaluation", () => {
    const run = () =>
      recommendWatchesV3(v3Profile(), seedCatalogue, {
        asOf: "2026-09-03T00:00:00.000Z",
      });
    expect(JSON.stringify(run())).toBe(JSON.stringify(run()));
  });

  it("never recommends a candidate carrying a hard reason", () => {
    const result = recommendWatchesV3(v3Profile(), seedCatalogue, {
      asOf: "2026-09-03T00:00:00.000Z",
    });
    for (const candidate of result.recommendations) {
      expect(candidate.hardReasons).toEqual([]);
      expect(candidate.missingFacts).toEqual([]);
    }
  });

  it("reports unpopulated soft dimensions as unscored", () => {
    const result = recommendWatchesV3(
      v3Profile({ caseShape: "round" }),
      seedCatalogue,
      { asOf: "2026-09-03T00:00:00.000Z" },
    );
    expect(
      result.unscoredPreferences.some((entry) => entry.field === "caseShape"),
    ).toBe(true);
  });

  it("carries the positioning line onto each candidate", () => {
    const result = recommendWatchesV3(v3Profile(), seedCatalogue, {
      asOf: "2026-09-03T00:00:00.000Z",
    });
    for (const candidate of result.recommendations) {
      expect(candidate).toHaveProperty("positioningLine");
      expect(candidate).toHaveProperty("positioningGroup");
    }
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/domain/recommendation.test.ts`
Expected: FAIL — `scoreVariantV3` is not exported.

- [ ] **Step 3: Write the implementation**

Append to `app/domain/recommendation.ts`. Follow the existing v2 helpers: reuse `addScore`, `compareCandidates`, and `diversify`; build candidates with the same field set plus `positioningLine` and `positioningGroup`.

Scoring rules:

- `budget_headroom` — `min(10, headroom * 20)` where headroom is the fraction of the ceiling left unspent, rounded to one decimal. Same as v2.
- `diameter_centring` — `max(0, 10 - |diameter - midpoint| * 2)` rounded to one decimal, where midpoint is `(caseDiameterMinMm + caseDiameterMaxMm) / 2`. Skipped when the diameter is unverified.
- `thickness_fit` — 8 points when `maxCaseThicknessMm` is set and the verified thickness is at or below it; 0 otherwise. Skipped when unset or unverified.
- `crystal_match` — 6 points on an exact match.
- `shape_match`, `display_caseback_match`, `construction_match`, `micro_adjustment_match` — 6, 4, 6, and 4 points on an exact match.
- `evidence_completeness` — `max(0, 8 - missingFacts.length * 2)`. Same as v2.

For `unscoredPreferences`, emit one entry per set soft preference whose catalogue field is `null` on every variant in the catalogue, using this explanation shape: `"No reviewed <field> data exists in the catalogue yet, so this preference did not affect the ranking."` Cover `caseShape`, `displayCaseback`, `movementConstruction`, and `microAdjustmentRequired`.

`recommendWatchesV3` mirrors `recommendWatches`: evaluate every variant, partition into rejected / verification-required / eligible, sort with `compareCandidates`, `diversify(eligible, 3)`, slice why-not and verification-required to 3, collect `sources` by candidate `sourceIds`, and return `diagnostics` with the same five counters. Set `engineVersion: RECOMMENDATION_V3_ENGINE_VERSION`.

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/domain/recommendation.test.ts`
Expected: PASS, including the 9 new tests.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/domain/recommendation.ts app/domain/recommendation.test.ts
git commit -m "$(cat <<'MSG'
add version-3 scoring and result assembly

Adds diameter centring, thickness, shape, caseback, construction,
crystal, and micro-adjustment factors, and reports soft preferences with
no catalogue data through the unscored channel.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 13: Quiz UI — six screens

**Files:**

- Modify: `app/routes/quiz.tsx`
- Modify: `app/routes/quiz.test.tsx`

**Interfaces:**

- Consumes: `profileV3Schema`, `QUESTIONNAIRE_V3_STORAGE_KEY` from Task 10; `recommendWatchesV3` from Task 12; `loadCatalogueVocabulary` from Task 7.
- Produces: a six-screen `/quiz` posting a v3 payload.

This is the swap point. Replace the `CORE_STEP_COUNT = 6` / `REFINE_STEP_COUNT = 7` machinery with `SCREEN_COUNT = 6` and `SUMMARY_STEP = 6`. Scenario and complication options come from the loader, not from a hardcoded list.

- [ ] **Step 1: Write the failing test**

Replace the screen-navigation and payload assertions in `app/routes/quiz.test.tsx` with:

```typescript
describe("quiz v3 action", () => {
  it("rejects a version-2 payload", async () => {
    const response = await action(
      buildRequest({ version: "2", budgetCurrency: "USD", budgetMax: "5000" }),
    );
    expect(response.status).toBe(400);
  });

  it("accepts a complete version-3 payload", async () => {
    const response = await action(
      buildRequest({
        version: "3",
        budgetCurrency: "USD",
        budgetMax: "15000",
        wearingScenarios: ["office"],
        minimumWaterResistanceM: "100",
        caseDiameterMinMm: "36",
        caseDiameterMaxMm: "41",
        movementTypes: ["automatic"],
        requiredComplications: [],
        allergyConstraint: "none",
      }),
    );
    expect(response.status).toBe(200);
  });

  it("rejects an inverted diameter range", async () => {
    const response = await action(
      buildRequest({
        version: "3",
        budgetCurrency: "USD",
        budgetMax: "15000",
        wearingScenarios: ["office"],
        minimumWaterResistanceM: "100",
        caseDiameterMinMm: "42",
        caseDiameterMaxMm: "38",
        movementTypes: ["automatic"],
        requiredComplications: [],
        allergyConstraint: "none",
      }),
    );
    expect(response.status).toBe(400);
  });
});

describe("quiz v3 screens", () => {
  it("renders six screens and no personal-profile stage", () => {
    render(<Quiz />, { wrapper: RouterWrapper });
    expect(screen.getByText(/1 of 6/i)).toBeInTheDocument();
    expect(screen.queryByText(/personal profile/i)).not.toBeInTheDocument();
  });

  it("offers scenario options supplied by the loader", () => {
    render(<Quiz />, { wrapper: RouterWrapper });
    expect(screen.getByRole("checkbox", { name: /office/i })).toBeInTheDocument();
  });
});
```

Adapt `buildRequest` and `RouterWrapper` to the helpers already present in the file.

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/routes/quiz.test.tsx`
Expected: FAIL — the action still validates v2 and the UI still renders 13 screens.

- [ ] **Step 3: Rewrite the route**

In `app/routes/quiz.tsx`:

1. Replace the step constants with `const SCREEN_COUNT = 6;` and `const SUMMARY_STEP = SCREEN_COUNT;`.
2. In `loader`, add `vocabulary: await loadCatalogueVocabulary()` alongside the existing access check, and expose only `slug` and `labelEn` for the `wearing_scenario` and `complication` kinds.
3. In `action`, parse the form with `profileV3Schema` and call `recommendWatchesV3`. Keep the existing rate-limit, access-grant, funnel-event, and dossier-delivery behaviour unchanged.
4. Replace the thirteen screen components with six, in the spec's order: Budget, Use, Case, Movement, Details, Requirements.
5. Every soft control gets an explicit "No preference" option; scenario and complication controls are checkbox groups built from `vocabulary`.
6. Change the stored draft key to `QUESTIONNAIRE_V3_STORAGE_KEY`. A draft under the v2 key is discarded rather than migrated — the shapes are not compatible.
7. Update the progress label to `Step {step + 1} of 6` and delete the "Essential fit" / "Personal profile" stage labels.

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/routes/quiz.test.tsx`
Expected: PASS.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app/routes/quiz.tsx app/routes/quiz.test.tsx
git commit -m "$(cat <<'MSG'
replace the diagnostic with six version-3 screens

Swaps the thirteen-screen flow for budget, use, case, movement, details,
and requirements. Scenario and complication options are supplied by the
vocabulary loader rather than hardcoded.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 14: Result-page positioning facet

**Files:**

- Create: `app/components/positioning-facet.tsx`
- Test: `app/components/positioning-facet.test.tsx`
- Modify: `app/routes/quiz.tsx`
- Modify: `app/styles/quiz.css`

**Interfaces:**

- Consumes: `VocabularyRow` from Task 1; the candidate `positioningGroup` field from Task 12.
- Produces: `<PositioningFacet groups={...} selected={...} onSelect={...} />` and a filtered result list.

Spec section 7: the facet narrows what is displayed, never re-ranks, and renders nothing when the vocabulary is empty.

- [ ] **Step 1: Write the failing test**

Create `app/components/positioning-facet.test.tsx`:

```typescript
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";

import { PositioningFacet } from "./positioning-facet";

const groups = [
  { slug: "instrument", labelEn: "Instrument" },
  { slug: "quiet_classic", labelEn: "Quiet classic" },
];

describe("PositioningFacet", () => {
  it("renders nothing when no groups are available", () => {
    const { container } = render(
      <PositioningFacet groups={[]} selected={null} onSelect={vi.fn()} />,
    );
    expect(container).toBeEmptyDOMElement();
  });

  it("renders a chip per group plus an all option", () => {
    render(
      <PositioningFacet groups={groups} selected={null} onSelect={vi.fn()} />,
    );
    expect(screen.getByRole("button", { name: "All" })).toBeInTheDocument();
    expect(
      screen.getByRole("button", { name: "Instrument" }),
    ).toBeInTheDocument();
  });

  it("marks the selected chip as pressed", () => {
    render(
      <PositioningFacet
        groups={groups}
        selected="instrument"
        onSelect={vi.fn()}
      />,
    );
    expect(screen.getByRole("button", { name: "Instrument" })).toHaveAttribute(
      "aria-pressed",
      "true",
    );
  });

  it("reports the chosen slug", async () => {
    const onSelect = vi.fn();
    render(
      <PositioningFacet groups={groups} selected={null} onSelect={onSelect} />,
    );
    await userEvent.click(screen.getByRole("button", { name: "Quiet classic" }));
    expect(onSelect).toHaveBeenCalledWith("quiet_classic");
  });

  it("reports null when All is chosen", async () => {
    const onSelect = vi.fn();
    render(
      <PositioningFacet
        groups={groups}
        selected="instrument"
        onSelect={onSelect}
      />,
    );
    await userEvent.click(screen.getByRole("button", { name: "All" }));
    expect(onSelect).toHaveBeenCalledWith(null);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/components/positioning-facet.test.tsx`
Expected: FAIL — module not found.

- [ ] **Step 3: Write the component**

Create `app/components/positioning-facet.tsx`:

```typescript
export type PositioningGroupOption = {
  slug: string;
  labelEn: string;
};

export function PositioningFacet({
  groups,
  selected,
  onSelect,
}: {
  groups: readonly PositioningGroupOption[];
  selected: string | null;
  onSelect: (slug: string | null) => void;
}) {
  if (groups.length === 0) return null;
  return (
    <div className="positioning-facet" role="group" aria-label="Positioning">
      <button
        type="button"
        aria-pressed={selected === null}
        onClick={() => onSelect(null)}
      >
        All
      </button>
      {groups.map((group) => (
        <button
          key={group.slug}
          type="button"
          aria-pressed={selected === group.slug}
          onClick={() => onSelect(group.slug)}
        >
          {group.labelEn}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Wire it into the result screen**

In `app/routes/quiz.tsx`, on the summary step:

1. Add `const [positioning, setPositioning] = useState<string | null>(null);`
2. Derive the groups from the loader vocabulary, keeping only `positioning_group` rows whose slug appears on at least one returned candidate.
3. Render `<PositioningFacet .../>` above the recommendation list.
4. Filter only the displayed recommendations: `positioning === null || candidate.positioningGroup === positioning`. Do not filter `verificationRequired`, `whyNot`, or re-sort anything.
5. Render each candidate's `positioningLine` verbatim on its card.
6. Add a `.positioning-facet` rule to `app/styles/quiz.css` following the existing chip patterns in that file.

- [ ] **Step 5: Run tests and commit**

Run: `npx vitest run app/components/positioning-facet.test.tsx app/routes/quiz.test.tsx`
Expected: PASS.

```bash
npm run check
git add app/components/positioning-facet.tsx app/components/positioning-facet.test.tsx app/routes/quiz.tsx app/styles/quiz.css
git commit -m "$(cat <<'MSG'
add result-page positioning facet

Narrows the displayed shortlist by positioning group without re-ranking
or altering hard-filter outcomes, and renders nothing when the
vocabulary is unavailable.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 15: Version-4 RPCs and parity

**Files:**

- Create: `db/migrations/0066_add_recommendation_v4_rpcs.sql`
- Modify: `app/domain/catalogue.server.ts`
- Modify: `app/domain/catalogue-parity.ts`
- Modify: `app/domain/evaluation-fixtures.ts`

**Interfaces:**

- Consumes: `evaluateHardFiltersV3` from Task 11.
- Produces: `recommendation_catalogue_v4`, `recommendation_hard_filter_v4`; `SUPABASE_CATALOGUE_RPC` and `SUPABASE_HARD_FILTER_RPC` repointed; v3 golden profiles.

The SQL predicate must mirror `evaluateHardFiltersV3` exactly, including reporting an unverified fact as missing rather than passing.

- [ ] **Step 1: Write the migration**

Create `db/migrations/0066_add_recommendation_v4_rpcs.sql`. `recommendation_catalogue_v4` returns the v3 catalogue payload plus the new columns and the scenario and complication slug arrays. `recommendation_hard_filter_v4` takes the v3 profile as `jsonb` and returns one row per variant with `hard_reasons text[]` and `missing_facts text[]`, applying the seven checks in the order used by `evaluateHardFiltersV3`.

Keep the v3 functions in place. Grant execute on the v4 functions to `anon` and `authenticated` only, matching the v3 grants in `0009_harden_recommendation_rpc_grants.sql`.

- [ ] **Step 2: Apply and verify the migration**

Apply through the Supabase MCP `apply_migration` tool with name `add_recommendation_v4_rpcs`. Verify:

```sql
select count(*) from public.recommendation_catalogue_v4();
```

Expected: matches the number of accepted variants.

- [ ] **Step 3: Repoint the server and rebuild the fixtures**

In `app/domain/catalogue.server.ts`, change the two constants to `recommendation_catalogue_v4` and `recommendation_hard_filter_v4`, and swap `HARD_REASON_CODES` / `MISSING_FACT_CODES` for their v3 counterparts in `hardFilterEvaluationSchema`.

In `app/domain/evaluation-fixtures.ts`, replace `goldenEvaluationProfiles` with six `ProfileV3` values covering: a permissive baseline, a tight budget, a narrow diameter range, a high water-resistance requirement, a quartz-only movement selection, and a nickel-allergy constraint.

In `app/domain/catalogue-parity.ts`, update `catalogueParityMismatches` to compare `evaluateHardFiltersV3` against the v4 RPC output.

- [ ] **Step 4: Run the parity audit**

Run: `npm run audit:catalogue-parity`
Expected: exit 0, no mismatches across all six profiles.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add db/migrations/0066_add_recommendation_v4_rpcs.sql app/domain/catalogue.server.ts app/domain/catalogue-parity.ts app/domain/evaluation-fixtures.ts
git commit -m "$(cat <<'MSG'
add version-4 catalogue and hard-filter RPCs

Mirrors the v3 TypeScript predicate in PostgreSQL, repoints the server
loader, and rebuilds the six golden profiles. The v3 RPCs remain for
rollback.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 16: Remove version-2 code

**Files:**

- Modify: `app/domain/questionnaire.ts`
- Modify: `app/domain/recommendation.ts`
- Modify: `app/domain/catalogue.ts`
- Modify: their test files
- Modify: `scripts/build-watches-models-workbook.ts`

**Interfaces:**

- Consumes: nothing new.
- Produces: a single questionnaire and engine version. `SOCIAL_SIGNALS`, `AESTHETIC_DNA`, `DEPLOYMENT_ENVIRONMENTS`, and `PRICE_COMFORTS` survive — the archetype quiz and discovery trait matching import them.

- [ ] **Step 1: Confirm what is still referenced**

Run:

```bash
grep -rn "coreProfileSchema\|refinementSchema\|questionnaireProfileSchema\|recommendWatches\b\|evaluateHardFilterPartition\|dateStatus\|eligibleEnvironments\|ACCURACY_TOLERANCES\|WEIGHT_LIMITS\|MAX_LUG_TO_LUG_TO_WRIST_RATIO" app scripts --include=*.ts --include=*.tsx | grep -v "\.test\."
```

Expected: only definitions remain, no live consumers outside the v2 modules themselves.

- [ ] **Step 2: Delete the v2 questionnaire surface**

From `app/domain/questionnaire.ts` remove: `QUESTIONNAIRE_VERSION`, `QUESTIONNAIRE_STORAGE_KEY`, `WRIST_BANDS`, `WRIST_UNITS`, `MILLIMETRES_PER_INCH`, `OWNERSHIP_FRICTION_LEVELS`, `ACCURACY_TOLERANCES`, `WEIGHT_LIMITS`, `WEIGHT_LIMIT_GRAMS`, `COMPLICATIONS`, `DATE_PREFERENCES`, `PROVENANCE_PREFERENCES`, `EMOTIONAL_OBJECTIVES`, `MARKET_STANCES`, `SPECULATIVE_RISK_TOLERANCES`, `LUG_CURVATURES`, `ATTACHMENT_TYPES`, `ACQUISITION_CHANNELS`, `AVAILABILITY_TOLERANCES`, `LIQUIDITY_PREFERENCES`, `LUME_PREFERENCES`, `CROWN_POSITIONS`, `COSMETIC_TOLERANCES`, `CONDITIONS`, `ALLERGY_CONSTRAINTS`, `coreProfileSchema`, `refinementSchema`, `questionnaireProfileSchema`, `wristCircumferenceToMm`, `deriveWristBand`, `permitsSpeculativeCandidate`, `normalizeProfile`.

Keep: `CURRENCIES`, `PRICE_BANDS`, `derivePriceBand`, `effectiveBudgetCeiling`, `SOCIAL_SIGNALS`, `AESTHETIC_DNA`, `DEPLOYMENT_ENVIRONMENTS`.

`catalogue.ts` imports `ACQUISITION_CHANNELS` and `CONDITIONS` for the price schema — move those two constants into `catalogue.ts` rather than deleting them.

- [ ] **Step 3: Delete the v2 engine and schema fields**

From `app/domain/recommendation.ts` remove `RECOMMENDATION_ENGINE_VERSION`, `MAX_LUG_TO_LUG_TO_WRIST_RATIO`, `HARD_REASON_CODES`, `MISSING_FACT_CODES`, `evaluateCoreHardFilters`, `evaluateRefinementHardFilters`, `scoreCandidate`, `evaluateVariant`, `evaluateHardFilterPartition`, `recommendWatches`, and their explanation maps.

Keep the `V3` suffix on the surviving symbols. Renaming them would cascade through `catalogue.server.ts`, `catalogue-parity.ts`, `evaluation-fixtures.ts`, and `quiz.tsx`, which Tasks 13 to 15 have already wired to the suffixed names.

From `app/domain/catalogue.ts` remove `dateStatus` and `eligibleEnvironments`, and delete `supportedAccuracyTolerances`, `verifiedCaseWearingSpanMm`, and their `EvidenceField` members if nothing references them.

Run `npm run migrate:seed-catalogue-v3` once more if the seed JSON still carries the removed keys, or strip them with a small node one-liner.

- [ ] **Step 4: Update the workbook exporter**

In `scripts/build-watches-models-workbook.ts`, replace the four blank columns with real values: case shape from `geometry.caseShape`, display-back from `materials.displayCaseback`, construction from `movement.construction` joined with the caliber, and micro-adjustment from `operation.microAdjustment`. Replace the `scenarios()` helper's three-value map with a lookup over the vocabulary labels, and emit `positioningLine` in the social-context column.

- [ ] **Step 5: Run the full gate and commit**

```bash
npm run check
git add app scripts data/catalogue/seed-catalogue.json
git commit -m "$(cat <<'MSG'
remove the version-2 questionnaire and engine

Deletes the superseded profile schemas, hard filters, scoring, date
status, and deployment environments, and updates the workbook exporter
to emit the real sheet fields. Archetype and discovery constants remain.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

### Task 17: Full verification and documentation

**Files:**

- Modify: `README.md`
- Modify: `docs/original-plan-requirements.md`
- Modify: `docs/session-handoff.md`
- Modify: `tests/` browser specs

**Interfaces:**

- Consumes: everything above.
- Produces: recorded evidence that every gate passes.

- [ ] **Step 1: Update the browser specs**

In the Playwright specs under `tests/`, replace the thirteen-screen walkthrough with the six-screen flow and assert that the positioning facet narrows the visible shortlist without changing the number of hard-filter rejections shown.

- [ ] **Step 2: Run every gate**

```bash
npm run check
npm run audit:catalogue-parity
npm run evaluate:baseline
npx playwright install chromium
npm run test:e2e
```

Expected: all pass. Record the exact counts.

- [ ] **Step 3: Run Supabase advisors**

Run the Supabase `get_advisors` tool for `security` and `performance`. Expected: no new critical finding for `catalogue_vocabulary`, the two join tables, or the v4 functions.

- [ ] **Step 4: Update the documentation**

- `README.md`: describe the six-screen diagnostic and the sheet-native catalogue; add `import:model-sheet` and `migrate:seed-catalogue-v3` to the commands list.
- `docs/original-plan-requirements.md`: add a dated "2026-09-03 owner questionnaire amendment" section recording that questionnaire v2 and its 21 personal dimensions are superseded for `/quiz`, that wrist-based fit filtering and speculative-candidate opt-in are removed, and that the archetype and discovery surfaces are unaffected. Link the spec.
- `docs/session-handoff.md`: replace the stale checkpoint with the real state — migrations through `0066`, quiz v3 live, engine v3, vocabulary in PostgreSQL, sheet importer, and any unresolved sheet conflicts from Task 9.

- [ ] **Step 5: Commit**

```bash
npm run check
git add README.md docs tests
git commit -m "$(cat <<'MSG'
record quiz v3 verification and amend the requirements ledger

Updates the browser specs for the six-screen flow, documents the
sheet-native catalogue commands, and records the owner amendment
superseding questionnaire v2 for the diagnostic.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
)"
```

---

## Follow-up plan

The cleanup workstream in spec section 11 — stale session handoff, undocumented movie-watch pipeline, four missing unit tests, dead `runConfiguredDiscoveryResearchBatch`, duplicated `brand-manifest.txt`, and the Perplexity contract alignment — is independent of this chain and gets its own plan. Task 17 covers only the session-handoff entries this work invalidates.
